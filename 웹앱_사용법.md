# 3DGS 다시점 거리 측정기 (웹앱) — 사용법

설치 없이 브라우저에서 3D Gaussian Splatting 파일을 열고, **같은 점을 여러 각도에서 클릭**해 3D 좌표와 두 점 사이 거리를 재는 웹앱입니다. 논문 Deng & Qin (2026) *Accurate Point Measurement in 3DGS* 의 다시점 공간교회(spatial intersection) 방법을 그대로 구현했고, 일반 사용자를 위해 **안내선·확대창·자동 정밀 보정**과 **"어떤 정보가 없어서 안 되는지" 정확히 알려 주는 오류 메시지**를 추가했습니다.

같은 내용의 웹 버전(앱 안에서 열림): [webapp/help.html](webapp/help.html) · 첫 방문 시 자동으로 기능 소개 투어가 실행됩니다.

## 1. 열기 — 세 가지 방법

| 방법 | 어떻게 | 비고 |
|---|---|---|
| **① 더블클릭 (가장 쉬움)** | **`webapp/dist/3DGS_거리측정기_단일파일_조작메뉴.html`** 을 더블클릭 (2026-09-19 최신: 플로팅 조작 패널 + 방향제어·GCP 보정 포함) · 이전 판 `…_방향제어.html`, `…_자동회전.html`, `…_단일파일.html` 도 유지 | 모든 코드가 한 파일(7 MB)에 들어 있어 인터넷·서버 없이 동작. 샘플 체험 버튼만 없음(내 파일은 정상) |
| **② 로컬 서버 스크립트** | 터미널에서 `~/storage/Cesium/tools/start_webapp.sh` 실행 → 브라우저가 `http://localhost:8000/` 을 자동으로 열음 | 샘플 체험 포함 전체 기능. 종료는 Ctrl+C |
| **③ 온라인 (GitHub Pages)** | **https://rocheol2.github.io/3DGS_Accurate_Point_Mesurement/** | 2026-09-05 배포 완료(`gh-pages` 브랜치). 주소만 열면 됨 |

> ⚠ **`webapp/index.html` 을 직접 더블클릭하면 동작하지 않습니다.** Chrome 이 `file://` 에서 모듈 스크립트(app.js)를 보안 정책으로 차단하기 때문입니다(콘솔에 "Cross origin requests are only supported for protocol schemes: http, https…"). 이 경우 화면에 위 세 방법을 안내하는 문구가 뜹니다. 버튼이 아무 반응이 없고 PLY 를 놓으면 파일이 새 탭으로 열리는 증상이 바로 이것입니다.

온라인 버전은 `gh-pages` 브랜치(= `webapp/` 폴더 내용)로 배포되어 있습니다. 웹앱을 수정한 뒤 다시 배포하려면 `tools/publish_pages.sh` 를 실행하세요(단일 파일 재빌드 → gh-pages 갱신 → 푸시).

Chrome(WebGL2) 권장.

## 2. 열 수 있는 파일

- `.ply` — 3DGS 원본(`point_cloud/iteration_30000/point_cloud.ply`), SuperSplat 압축 PLY
- `.spz`, `.splat`, `.ksplat`, `.sog`/`.zip`
- `.json` 사이드카 — 위 파일과 **함께** 놓으면 축척 정보로 읽음: `{"units":"m"}` 또는 `{"scale_to_meters":4.2015}`, 또는 `tools/gs_ply_georef.py` 가 만든 메타 JSON
- 열 수 없는 것: `input.ply`/`points3D.ply`(일반 점군, E03), 2DGS 원본 PLY(E04), 메시·사진(E02)

샘플: 시작 화면의 **[▶ 샘플로 체험]** — 진주 LH 현장 15만 가우시안, 미터 단위·Z-up (`webapp/sample/`).

## 3. 따라하기

1. **파일 열기** — 끌어다 놓기 또는 [📂 파일 열기]. 뒤집혀 보이면 [⤒ 위 방향]에서 −Y/+Y/+Z 변경(측정에는 영향 없음).
2. **다가가기** — 휠: **커서가 가리키는 표면을 향해** 확대(남은 거리의 12 %씩 접근, 표면 통과 없음), 왼쫁 드래그: 회전, 오른쪽 드래그: 이동.
3. **[● 점 측정]**(M) → 잴 점을 크게 확대해 **한 번 클릭**. 커서 옆 확대창이 픽셀을 보여 줍니다. 노란 광선 1개가 생기고 화면이 그 점을 중심으로 정렬됩니다.
4. **카메라를 20° 이상 돌리기** — 왼쪽 드래그 또는 **[↻ 자동 회전]**(R). 오른쪽 패널의 각도 막대가 초록이 되면 충분. 첫 광선이 **노란 점선(안내선)** 으로 그려집니다.
5. **안내선 위에서 같은 점 클릭** — 30 px 이내면 선에 스냅, 첫 클릭 주변 무늬와 비교해 자동 정밀 보정(광선 표에 보정량·유사도 표시). 2개째부터 **청록 원(현재 추정점)** 근처를 클릭하면 됩니다.
6. **5개 채우면 자동 확정**(설정에서 2~12, Enter 로 조기 확정). 결과 탭에 P1 이 좌표·σ·품질과 함께 추가. 품질이 빨강이면 [✂ 최악 광선 제거] 또는 Esc 후 다시.
7. **거리** — **[↔ 거리 측정]**(D) 으로 A, B 를 차례로 재면 화면과 결과 탭에 `거리 ± 오차` 표시. 이미 잰 점 2개를 체크하고 [선택 두 점 거리]도 가능.

### 3-1. 자동 회전 (2026-09-19 추가, 켬/끔 가능)
측정 패널의 **자동 회전 [켬]** (또는 설정 탭 "클릭 후 자동 회전")이 켜져 있으면, 점을 클릭할 때마다 카메라가 **첫 클릭 지점을 중심으로 350° ÷ 광선 수** 만큼(기본 5개 → 70°) 자동으로 돌아갑니다. 거리와 높이는 그대로 두고 방위각만 바꾸며, 첫 클릭 지점(정확히는 커서 아래 표면 점, 2번째부터는 현재 추정점)이 **화면 중앙에 고정**되므로 사용자는 화면 중앙 근처의 같은 점을 **다시 클릭만** 하면 됩니다. 그 각도에서 점이 가려져 보이면 **R** 을 눌러 한 번 더 돌리거나 직접 드래그하세요. 회전 애니메이션(0.7초) 중의 클릭은 무시됩니다.

#### 회전 방향·각도·축 정하기 (방향제어 판)
측정 패널의 **방향 / 각도 / 축** 으로 자동 회전을 원하는 대로 바꿉니다.
- **방향**: ⟳ 오른쪽으로 계속 · ⟲ 왼쪽으로 계속 · ⇄ 좌우 번갈아(+α, −α, +2α, −2α: 처음 본 방향에서 크게 벗어나지 않아 점을 알아보기 쉬움) · ⇅ 위아래 번갈아.
- **각도**: 비우면 350° ÷ 광선 수(기본 70°). 20~45° 처럼 작게 넣으면 점이 덜 변해 찾기 쉽고, 대신 광선 각도가 작아집니다(20° 이상 권장).
- **축**: **화면 기준**(기본) = 지금 보는 화면의 세로축(좌우 회전)·가로축(상하 회전) 기준으로 돕니다. **파일 위(up) 축** = 파일의 하늘 방향을 축으로 돕니다(위 방향 설정이 맞을 때 수평 궤도).
- **회전 중심**은 항상 측정 중인 점(첫 클릭은 커서 아래 표면 점, 2번째부터는 추정점)이며 회전 후에도 화면 중앙에 있습니다.
- **← → ↑ ↓ 키** 또는 조작 패널 버튼: 측정 중 그 방향으로 한 단계 회전(위아래는 최대 45°). 자동 회전이 마음에 안 드는 각도로 갔을 때 직접 고칩니다. 코치 바에 "다음 회전: ⟳ 오른쪽 70°" 처럼 다음 동작이 표시됩니다.

### 3-1-1. 플로팅 조작 패널 (조작메뉴 판) — 마우스 없이 회전·확대·이동
파일을 열면 3D 화면 왼쪽 아래에 **🎮 조작** 패널이 뜹니다(도구막대의 [🎮 조작 패널] 또는 설정 탭에서 켜고 끔).
- **회전** ◀ ▶ ▲ ▼: 궤도 중심(측정 중이면 측정 중인 점)을 기준으로 한 번에 15°(설정에서 변경). 가운데 ◎ 는 측정 중인 점에 초점.
- **확대** ＋ －: 화면 중앙의 표면을 향해 접근/후퇴(휠과 같은 표면 인식 방식이라 통과하지 않음).
- **이동** ← → ↑ ↓: 화면 기준 평행이동(궤도 중심 거리의 12 %씩). 가운데 ⌂ 는 모델 전체 보기.
- 버튼을 **누르고 있으면** 0.3초 뒤부터 천천히 연속 동작(미세 조정), 제목을 끌어 원하는 위치로 옮길 수 있고 ▾ 로 접습니다. 위치는 브라우저에 기억됩니다.

### 3-2. 확대창 크기·화질 (2026-09-19 추가)
측정 패널의 **확대창 [소/중/대]** (160/260/380 px) 로 확대창 크기를 바꿀 수 있습니다(설정 탭에도 동일). 배율은 기본 **2배**(2026-09-19 부터, 이전 4배)이며 설정 탭 슬라이더(1~8배, 0.5 단위)나 `[` `]` 키로 조절합니다. **고해상도 렌더링**(기본 켬)은 확대 영역만 카메라를 좁혀 별도로 다시 그리므로, 화면 픽셀을 늘리는 방식보다 훨씬 선명합니다(확대창 하단에 "고해상도" 표시). GPU 부담이 커 느리면 설정에서 끄면 됩니다. 실패 시 자동으로 픽셀 복사 방식으로 전환됩니다.

## 3-3. 지역·국가 좌표계 PLY (큰 좌표) 바로 열기 (2026-09-19 추가)
GCP 지오리퍼런싱으로 좌표가 수십만 m(예: EPSG:5186 X≈285,900)인 PLY는 GPU 의 float32 정밀도 한계로 렌더링이 깨지거나 열리지 않았습니다. 이제 뷰어가 파일을 읽으면서 **좌표가 크면(중앙값이 2,000 u 이상이고 장면 크기의 200배 이상) 자동으로 원점을 옮겨** 렌더링합니다(E14 안내). 화면에 표시되는 좌표, CSV/JSON 내보내기, 기준점(GCP) 보정 입력은 모두 **원래 좌표**를 그대로 씁니다. 오프셋 값은 [정보 점검]에서 볼 수 있습니다. 더 이상 파일을 미리 offset 해서 저장할 필요가 없습니다. (SuperSplat 압축 PLY 도 chunk 경계를 옮겨 처리. SPZ 등 다른 형식은 해당 없음.)

## 4. 축척(미터) — 없으면 없다고 알려 줍니다

COLMAP 기반 3DGS 는 크기 정보가 없습니다. 파일을 열 때 축척 정보가 없으면 **주황 배너(E05)** 가 "미터 거리를 표시할 수 없다"는 사실·이유·해결 방법 3가지를 보여 주고, 거리는 **모델 단위(u)** 로 ⚠ 표시와 함께 나옵니다.

[📏 축척 보정]을 누르면 세 가지 방법을 고르는 화면이 뜹니다.

1. **① 길이 하나로** — 실제 길이를 아는 구간(A4 긴 변 297 mm, 줌자로 잰 문 폭, GCP 두 점 거리)의 참값 입력 → 양 끝점을 보통처럼 측정 → `축척 = 참값 ÷ 측정값` 적용, 상대 불확도 표시. 파일(이름+크기)별로 브라우저에 저장되어 다음에 자동 적용. [적용 + 사이드카 JSON 저장]으로 다른 PC 에서도 재사용.
2. **② 기준점(GCP) 좌표로** — 실제 좌표를 아는 지점(측량 기준점, GCP 표지)을 **2개 이상 이 앱에서 측정**한 뒤, 표에 각 점의 실제 X·Y·Z 를 입력하고 **입력 단위(m/cm/mm)** 와 좌표계 이름(예: EPSG:5186)을 고릅니다. 국가좌표처럼 큰 숫자(285889.063)도 그대로 넣습니다. 2점이면 축척만, **3점 이상(한 줄 위가 아닌)이면 축척 + 회전 + 이동**을 최소제곱으로 구해 이후 모든 점 좌표를 실제 좌표계로 표시하고 CSV 에 실좌표 열(x_real, y_real, z_real, crs)을 추가합니다. 점별 잔차와 RMS 로 입력 오류(X/Y 바뀜, 단위)를 잡을 수 있습니다.
3. **③ 축척 직접 입력** — "모델 1 u = 실제 몇 m" 를 아는 경우(예: `gs_ply_georef.py` 출력 4.2015, GCP Scale 9.7429).
4. **사이드카 JSON / PLY 헤더 주석** — `units: meters` 가 있으면 자동 인식. 기준점 보정을 [적용 + 사이드카 JSON 저장]으로 내보내면 변환(회전·이동)까지 JSON 에 담겨 다른 PC 에서도 그대로 씁니다.

## 5. 결과 읽기

| 표시 | 뜻 | 기준 |
|---|---|---|
| σ₀ | 광선들이 계산된 점에서 평균 얼마나 벗어났나 | 작을수록 좋음 |
| 잔차 RMS(px) | 벗어남을 화면 픽셀로 환산 | ≤1.5 좋음 · ≤4 보통 · 초과 나쁨 |
| 최대 각도 | 가장 벌어진 두 광선 사이 각 | ≥20° 좋음 · 10~20° 보통 · <10° 확정 차단(E08) |
| 거리 ± | 두 점 공분산을 거리 방향으로 투영한 1σ (+축척 불확도) | |

## 6. 내보내기

[⬇ 내보내기] → **CSV**(점: 모델 단위·미터 좌표, σ, 광선 수, 품질 / 거리: u·m, 축척 출처), **JSON**(공분산·광선 포함), **PNG**(화면+라벨). 축척이 없으면 미터 열은 비어 있고 출처 열에 "축척 정보 없음"이 남습니다.

## 7. 마우스·키보드

왼쪽 드래그 회전 · 휠 = 커서 아래 표면으로 접근/후퇴(표면 통과 없음) · 오른쪽 드래그 이동 · 측정 중 클릭 = 광선 추가 · **M** 점 측정 · **D** 거리 측정 · **R** 다음 각도 · **Enter** 확정 · **Esc** 취소 · **Backspace** 마지막 광선 취소 · **F** 추정점에 초점 · **H** 홈 · **[ ]** 확대창 배율 · **?** 도움말

## 8. 오류·경고 메시지

앱의 모든 메시지는 `webapp/errors.js` 한 곳에 정의되어 있고(E01~E13), 도움말 페이지 9장에 전체 표가 자동 생성됩니다. 요약:

| 코드 | 수준 | 내용 |
|---|---|---|
| E01 | 진행 불가 | WebGL2 사용 불가 |
| E02 | 진행 불가 | 지원하지 않는 파일 형식 |
| E03 | 진행 불가 | 3DGS 가 아닌 일반 점군 PLY (input.ply 등) |
| E04 | 진행 불가 | 2DGS PLY (scale_2 없음) → 변환본 사용 |
| E05 | 주의 | **축척 정보 없음 → 미터 표시 불가**, 해결 3가지 안내 |
| E06 | 안내 | 위 방향 정보 없음 → 기본값 가정 |
| E07 | 진행 불가 | 광선 2개 미만 |
| E08 | 진행 불가 | 광선 각도 부족(<10°) → 깊이 결정 불가 |
| E09 | 주의 | 광선 잔차 과대(잘못 클릭 의심) |
| E10 | 진행 불가 | 축척 보정 계산 불가(거리 0, 참값 무효) |
| E11 | 진행 불가 | 파일 읽기 실패(메모리/손상) |
| E12 | 주의 | 사이드카 JSON 해석 불가 |
| E13 | 주의 | 파일이 매우 큼(300만 가우시안 또는 700 MB 초과) |
| E14 | 안내 | 좌표가 커서 뷰어 내부 원점 이동(표시 좌표는 원래 값) |

## 9. 자주 묻는 질문

- **Cesium 을 설치해야 하나요?** 아니요. 이 앱은 Cesium 을 쓰지 않습니다. 웹페이지 하나이며 렌더링은 Spark(three.js) 라이브러리가 페이지 안에서 합니다.
- **파일이 서버로 올라가나요?** 아니요. 브라우저 메모리에서만 읽습니다.
- **정확도는?** 합성 정육면체(모서리 2.000 m) 테스트에서 5시점 클릭의 3D 오차 2~4 mm, 두 꼭짓점 거리 2.002 ± 0.004 m. 실제 정확도 = 클릭 정밀도 + 축척 정확도 × 거리.
- **결과 좌표계는?** 파일(PLY) 자체의 좌표계 × 축척. 절대 좌표(위경도)가 필요하면 파일을 먼저 지오리퍼런싱하세요(`tools/gs_ply_georef.py`).
- **버튼을 눌러도 아무 반응이 없어요 / PLY 를 놓으면 파일 내용이 새 탭에 열려요.** `index.html` 을 더블클릭(file://)해 연 경우입니다. 1장의 ① 단일 파일 버전을 더블클릭하거나 ② `start_webapp.sh` 를 쓰세요.
- **1.4 GB PLY(586만 가우시안)가 안 열려요.** 브라우저 메모리 한계에 가깝습니다(E13 경고). 가우시안 수를 줄인 파일을 쓰세요. DS003 기숙사 모델은 이미 준비되어 있습니다: `~/storage/Cesium/data/ds003/ds003_cm002_local_metric_1p5M_sh0.ply` (GCP 축척 9.7429 적용 → 미터 단위, 150만 가우시안, 약 100 MB). 다른 파일은 `npx 3dgs-ply-3dtiles-converter@0.6.5 simplify --target-count 1500000 --sh 0 입력.ply 출력.ply`.
- **휠을 굴려도 어느 정도 이상 확대가 안 돼요 / 확대하다 갑자기 모델이 사라져요.** 2026-09-05 수정 전 버전의 증상입니다. 이전에는 카메라가 표면 깊이를 몰라 뚫고 지나갔습니다. 현재 버전은 커서 아래 가우시안들의 깊이로 표면을 추정해 그 앞까지만 접근합니다. 단일 파일 버전을 다시 내려받거나 온라인 주소를 새로고침하세요.
