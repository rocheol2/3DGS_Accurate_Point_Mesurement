# 3DGS 거리 측정 — 쉬운 설명 (하나씩 따라하기 판)

> 이 문서는 [3DGS_거리측정_튜토리얼.md](3DGS_거리측정_튜토리얼.md)(전문가용)을 **처음 하는 사람도 한 동작씩 따라갈 수 있게** 다시 쓴 것입니다.
> 어려운 낱말은 처음 나올 때 풀어 쓰고, 명령은 **한 번에 한 줄**, 그 다음에 **화면에 무엇이 보여야 하는지**를 적었습니다.
> 순서대로 하되, 이미 준비된 부분(표시: ✅)은 건너뛰어도 됩니다.

---

## 0장. 시작 전에 알아야 할 것 — 등장인물 소개

### 0-1. 우리가 하려는 일을 한 문장으로
"**3D 사진(3DGS 모델) 속에서 두 지점을 골라, 그 사이가 실제로 몇 미터인지 재는 것**"입니다.

### 0-2. 논문의 아이디어 (비유로)
어두운 방에서 어떤 물건의 위치를 알고 싶다고 합시다. 한 자리에서 손가락으로 가리키면 "저 방향 어딘가"만 알 수 있습니다. 하지만 **방 안의 서로 다른 자리 5곳에서 같은 물건을 가리키면**, 다섯 손가락이 뻗는 직선들이 **한 점에서 만나고**, 그곳이 물건의 진짜 위치입니다.

논문의 방법이 정확히 이것입니다.
- 3D 사진을 화면에서 돌려 보면서, **같은 점을 서로 다른 각도에서 5번 클릭**합니다.
- 클릭할 때마다 "내 눈(카메라) → 클릭한 곳" 방향의 **직선(광선)** 이 하나 생깁니다.
- 컴퓨터가 5개 직선에 **가장 가까운 한 점**을 계산합니다. 이게 측정된 점입니다.
- 직선들이 서로 얼마나 어긋났는지(σ, 시그마)도 같이 알려 주므로, 잘못 클릭했으면 바로 티가 납니다.

그래서 이 방법은 **모델 표면이 울퉁불퉁하거나 구멍이 나 있어도** 상관없습니다. 눈에 보이기만 하면 잴 수 있습니다. 논문에서는 이렇게 잰 점이 1~2 cm 안에 들어왔습니다.

### 0-3. 등장하는 것들 (프로그램인지, 파일인지, 어디서 받는지)

| 이름 | 정체 | 프로그램? 파일? | 설치/다운로드 | 이 작업에서 역할 |
|---|---|---|---|---|
| **3DGS 모델 (PLY 파일)** | 사진 수백 장으로 만든 "3D 사진". 색깔 있는 작은 타원(가우시안) 255만 개가 들어 있음 | 파일 (`point_cloud.ply`, 604 MB) | 이미 있음 (`~/storage/03_Results/...`) | 측정 대상 |
| **COLMAP** | 사진들에서 카메라 위치를 계산해 준 프로그램. 3DGS 학습 전에 이미 돌렸음 | 프로그램 (이미 사용 끝) | 이미 있음 | 카메라 위치 파일 `images.bin` 을 축척 계산에 씀 |
| **축척(스케일)** | 모델 속 "1" 이 실제로 몇 미터인지. COLMAP 은 이걸 몰라서 모델 크기가 임의임 | 개념 | — | **가장 중요.** 이걸 맞추지 않으면 거리가 무의미 |
| **gs_ply_georef.py** | 제가 만든 **파이썬** 스크립트. 드론 사진의 GPS 로 축척을 알아내고, 모델을 미터 단위·바로 선 자세로 바꿔 새 PLY 로 저장 | 파이썬 파일 (`tools/`) | 이미 있음 | 3장 |
| **Cesium (세슘)** | **별도 프로그램이 아닙니다.** 웹 브라우저(크롬) 안에서 지구를 3D 로 그려 주는 자바스크립트 "부품(라이브러리)". 구글어스 웹판 같은 것 | 웹페이지 안에 들어 있는 코드 | 설치 불필요. 논문 도구 안에 이미 포함(`npm install` 로 받아짐) | 3D 사진을 지구 위에 띄우고 클릭을 받아 계산 |
| **논문 측정 도구 (3dgs_measurement_tool)** | 논문 저자들이 Cesium 부품으로 만든 **측정용 웹페이지**. GitHub 에 공개 | 웹페이지 소스 (폴더) | 이미 받아 놓음 (`~/storage/Cesium/3dgs_measurement_tool`) | 5~7장. 내 컴퓨터에서 켜고 **크롬으로 접속** |
| **3D Tiles** | Cesium 이 큰 3D 데이터를 읽는 **파일 형식**. 데이터를 작은 조각(타일)으로 잘라 두어 필요한 부분만 불러옴 | 파일 묶음 (`tileset.json` + `tiles/*.glb`) | 변환기로 만듦 | Cesium 은 PLY 를 직접 못 읽어서 이 형식이 필요 |
| **변환기 (3dgs-ply-3dtiles-converter)** | PLY → 3D Tiles 변환 **명령줄 도구**. 파이썬도, 세슘 프로그램도 아니고 **Node.js 로 만든 오픈소스** | 명령 (터미널에서 실행) | **따로 다운로드할 필요 없음.** `npx` 명령이 처음 실행할 때 인터넷에서 자동으로 받아 옴 (이미 받아짐) | 4장 |
| **Node.js / npm / npx** | Node.js = 자바스크립트를 브라우저 밖(터미널)에서 돌리는 엔진. npm = 자바스크립트 부품 가게. npx = 가게에서 받아 바로 실행 | 프로그램 | conda 환경 `cesium_measure` 에 설치 완료 (Node 22) | 변환기 실행, 측정 웹페이지 켜기 |
| **conda** | 파이썬·Node 같은 도구를 "방(환경)" 별로 따로 두는 관리 프로그램. `conda activate 방이름` 으로 그 방에 들어감 | 프로그램 | 이미 있음 (miniconda3) | 1장에서 `cesium_measure` 방에 들어감 |
| **터미널** | 글자로 명령을 치는 창 | 프로그램 | Ubuntu 기본 | 모든 명령을 여기서 침 |
| **measure_distance.py** | 제가 만든 **파이썬** 스크립트. 도구가 내보낸 점 좌표 파일(CSV)에서 거리를 계산 | 파이썬 파일 (`tools/`) | 이미 있음 | 7~8장 |

> **정리:** "세슘 프로그램" 같은 건 설치할 게 없습니다. 우리가 **직접 켜는 것은 딱 하나**, 논문 도구(웹페이지)를 내 컴퓨터에서 돌리는 작은 서버이고, 그걸 **크롬으로 봅니다.** 변환은 **터미널 명령 한 줄**(Node.js 도구)이고, 축척 맞추기와 거리 계산은 **파이썬 스크립트 한 줄**씩입니다.

### 0-4. 전체 순서 (6칸)

```
[1] 터미널 열고 conda 방에 들어가기
[2] 내 PLY 파일 확인
[3] 파이썬 스크립트로 모델을 "미터 단위 + 바로 선 자세"로 바꾸기   (9초)
[4] 변환기로 PLY → 3D Tiles                                        (34초)
[5] 측정 웹페이지 켜고 크롬으로 열기
[6] 같은 점을 5번 클릭해 점 재기 → 두 점 재기 → CSV 저장 → 파이썬으로 거리 계산
```

### 0-5. 지금 어디까지 되어 있나 (2026-09-05 기준)

| 단계 | Site_Research_01 (LH 드론 현장) | 다른 모델 |
|---|---|---|
| [1] 환경 | ✅ 준비됨 | ✅ 같음 |
| [2] PLY 확인 | ✅ 확인함 | 직접 |
| [3] 미터 변환 | ✅ 완료 (`data/site01/site01_enu.ply`) | 직접 (3장) |
| [4] 3D Tiles | ✅ 완료 (`3dgs_measurement_tool/public/models/site01/`) | 직접 (4장) |
| [5] 웹페이지 등록 | ✅ 완료 (기본 모델로 등록됨) | 직접 (4장 끝) |
| [6] 측정 | **여기부터 직접** | 직접 |

→ **Site_Research_01 로 연습하려면 1장 → 5장으로 바로 가세요.** 3·4장은 다른 모델을 할 때, 또는 연습으로 다시 돌려도 됩니다(같은 결과가 나오고 덮어씁니다).

---

## 1장. 준비운동 — 터미널 열고 방에 들어가기

**1-1.** 키보드에서 `Ctrl` + `Alt` + `T` 를 누릅니다. → 검은 창(터미널)이 열립니다. 이 문서의 명령은 **복사해서 터미널에 붙여넣기**(`Ctrl`+`Shift`+`V`) 하고 `Enter` 를 칩니다.

**1-2.** Node 가 들어 있는 방에 들어갑니다.
```bash
conda activate cesium_measure
```
→ 보여야 하는 것: 줄 맨 앞이 `(cesium_measure)` 로 바뀝니다. 예: `(cesium_measure) rocheol2@pc:~$`

**1-3.** 방에 잘 들어갔는지 확인합니다.
```bash
node --version
```
→ 보여야 하는 것: `v22.23.2` (숫자가 v22 로 시작하면 OK). `command not found` 가 나오면 1-2 를 다시.

**1-4.** 작업 폴더로 이동하고 내용을 봅니다.
```bash
cd ~/storage/Cesium
ls
```
→ 보여야 하는 것 (이런 이름들):
```
3DGS_거리측정_튜토리얼.md  3DGS_거리측정_쉬운설명.md  3dgs_measurement_tool  data  patches  README.md  setup.sh  tools
```

> 💡 터미널에서 `~` 는 내 홈 폴더(`/home/rocheol2`)를 뜻합니다. `~/storage/Cesium` = `/home/rocheol2/storage/Cesium`.
> 💡 명령이 끝났는지는 **줄 맨 앞에 다시 `(cesium_measure) ...$` 가 나오는지**로 압니다. 그 전엔 기다리세요.

---

## 2장. 내 3DGS 파일 확인하기

**2-1. 어떤 파일을 써야 하나요?**
3DGS 학습이 끝난 폴더 안의 **`point_cloud/iteration_30000/point_cloud.ply`** 입니다.
- Site_Research_01 의 경우: `~/storage/03_Results/Vanilla_3DGS/Site_Research_01/point_cloud/iteration_30000/point_cloud.ply`
- ❌ 같은 폴더에 있는 `input.ply` 는 아닙니다(학습 시작용 일반 점). ❌ `iteration_7000` 은 덜 학습된 중간 결과입니다.
- ❌ 2DGS 결과의 `point_cloud.ply` 는 형식이 달라 안 됩니다 → `point_cloud_3dgs.ply` 라는 변환본을 쓰세요(`3DGS_DATA/tools/convert_2dgs_to_3dgs_ply.py` 로 만든 것).

**2-2.** 파일이 제대로인지 스크립트로 들여다봅니다.
```bash
python3 tools/gs_ply_georef.py info ~/storage/03_Results/Vanilla_3DGS/Site_Research_01/point_cloud/iteration_30000/point_cloud.ply
```
→ 보여야 하는 것:
```
가우시안 수: 2,551,677
속성 62개: ['x', 'y', 'z', ...] ... ['opacity', 'scale_0', 'scale_1', 'scale_2', 'rot_0', 'rot_1', 'rot_2', 'rot_3']
위치 1~99% 범위 (단위: 모델 단위): [67.38 32.82 56.44]
SH 차수: 3
```

**2-3. 읽는 법**
- `scale_0~2`, `rot_0~3`, `opacity` 가 있으면 3DGS 파일이 맞습니다.
- "위치 범위 67 × 33 × 56" 은 **미터가 아닙니다.** COLMAP 이 정한 임의 단위입니다. 실제 현장은 이보다 4배쯤 큽니다. 이걸 미터로 바꾸는 게 다음 장입니다.

---

## 3장. 모델을 "진짜 크기(미터)" 로 만들기 ★ 가장 중요

### 3-1. 왜 해야 하나요?
COLMAP 이 만든 모델은 **축척 표시가 없는 지도**와 같습니다. 모양은 맞지만 "1" 이 1 m 인지 4 m 인지 모릅니다. 그리고 "위" 방향도 정확히 하늘을 향하지 않고 약간 기울어져 있습니다(Site01 은 24° 기울어져 있었습니다).

해결: 드론 사진마다 **GPS 좌표(위도·경도·고도)** 가 기록되어 있습니다. COLMAP 이 계산한 카메라 위치(모델 단위)와 GPS 위치(미터)를 191장 짝지어 겹쳐 보면 "모델 1단위 = 몇 미터", "어느 쪽이 위" 가 나옵니다. 스크립트가 이걸 자동으로 하고, 모델 전체를 미터 단위·바로 선 자세로 고쳐 **새 파일**로 저장합니다(원본은 그대로 둠).

### 3-2. 명령 (Site_Research_01 예)
아래는 **한 명령**을 보기 좋게 4줄로 나눈 것입니다(줄 끝 `\` 가 "다음 줄에 계속" 표시). 통째로 복사해 붙이면 됩니다.
```bash
python3 tools/gs_ply_georef.py from-gps \
  --ply    ~/storage/03_Results/Vanilla_3DGS/Site_Research_01/point_cloud/iteration_30000/point_cloud.ply \
  --images ~/storage/02_Processed/Site_Research_01/3dgs_source/sparse/0/images.bin \
  --photos ~/storage/01_Raw/Photos/01_LH \
  --out    data/site01/site01_enu.ply
```

| 부분 | 뜻 |
|---|---|
| `from-gps` | "GPS 로 축척을 알아내는 방식으로" |
| `--ply ...` | 고칠 원본 3DGS 파일 (2장에서 확인한 것) |
| `--images .../sparse/0/images.bin` | COLMAP 이 계산한 **카메라 위치 파일**. 3DGS 학습에 쓴 폴더 안 `sparse/0/` 에 있음 |
| `--photos ~/storage/01_Raw/Photos/01_LH` | **GPS 가 살아 있는 원본 드론 사진 폴더.** ⚠️ 학습용 `images/` 폴더 사진은 GPS 정보가 지워져 있어 안 됨. 반드시 카메라에서 바로 나온 원본 |
| `--out data/site01/site01_enu.ply` | 저장할 새 파일 이름 (폴더는 자동 생성 안 되니 미리 `mkdir -p data/site01`) |

### 3-3. 실행하면 (약 9초)
→ 보여야 하는 것 (2026-09-05 실제 결과):
```
PLY 가우시안 2,551,677개
COLMAP 등록 이미지 191장
GPS 매칭 191장 | 고도 소스: xmp-relative
강건 적합(상위 85% 유지, 2회): 축척 4.2015 m/단위, 잔차 RMS 0.35 m, 사용 162장
  원본 좌표계에서의 위(Up) 방향: [ 0.009 -0.914  0.405] (기울기 23.9° vs -Y축)
  축척 상대 불확도(대략): 2.2 %
지면(2퍼센타일) 높이 U ≈ -4.9 m → 권장 --coordinate 높이 5.9 m
  SH 계수 회전 적용 (deg=3, K=15)
저장: /home/rocheol2/storage/Cesium/data/site01/site01_enu.ply
다음 단계 (3D Tiles 변환):
  3dgs-ply-3dtiles-converter ".../site01_enu.ply" <출력폴더> --coordinate "[35.17779716,128.14690829,5.9]" --no-open-inspector
```

### 3-4. 읽는 법
- **축척 4.2015 m/단위**: 모델의 "1" 이 4.2 m 였다는 뜻. 이제 새 파일은 미터 단위입니다.
- **잔차 RMS 0.35 m**: GPS 와 카메라 위치가 평균 35 cm 어긋남. 드론 GPS 는 원래 1~2 m 오차라 이 정도면 정상. 이 때문에 **축척에 약 1~2 % 오차**가 남습니다(10 m 를 재면 ±10~20 cm). 더 정확히 하려면 8장(실측 길이로 보정)을 합니다.
- **권장 높이 5.9 m**: 4장의 변환 명령에 넣을 숫자. 마지막 줄에 **다음에 칠 명령이 그대로 출력**되니 그걸 쓰면 됩니다.

**3-5.** 결과 파일 두 개가 생겼는지 봅니다.
```bash
ls -lh data/site01/
```
→ `site01_enu.ply` (604 MB) 와 `site01_enu.ply.json` (설정 기록) 두 개.

### 3-6. GPS 가 없거나, 축척을 이미 알면?
- **GCP(측량 기준점)로 축척을 이미 아는 경우**(DS003 기숙사 데이터: 축척 9.7429): `from-gps` 대신 `apply --scale 9.742857` 을 씁니다. 자세한 명령은 전문가용 튜토리얼 2단계 "경로 B".
- **아무 정보도 없는 경우**: 이 단계를 건너뛰고 4장부터 진행한 뒤, 8장에서 **줌자로 실측한 길이** 하나로 축척을 구합니다.

---

## 4장. PLY → 3D Tiles 로 바꾸기

### 4-1. 왜?
Cesium(웹페이지 속 3D 지구)은 PLY 파일을 직접 읽지 못합니다. **3D Tiles** 라는 "조각으로 나눈 형식" 만 읽습니다. 변환기는 인터넷의 오픈소스 도구인데, **`npx` 가 자동으로 받아 실행**하므로 따로 다운로드하지 않습니다.

### 4-2. 명령 (1장에서 `conda activate cesium_measure` 한 상태여야 함)
```bash
npx --yes 3dgs-ply-3dtiles-converter@0.6.5 \
  data/site01/site01_enu.ply \
  3dgs_measurement_tool/public/models/site01 \
  --coordinate "[35.17779716,128.14690829,5.9]" \
  --no-open-inspector --memory-budget 16
```

| 부분 | 뜻 |
|---|---|
| `npx --yes 3dgs-ply-3dtiles-converter@0.6.5` | "변환기 0.6.5 버전을 (없으면 받아서) 실행" |
| `data/site01/site01_enu.ply` | 3장에서 만든 미터 단위 PLY |
| `3dgs_measurement_tool/public/models/site01` | 결과를 넣을 폴더. **측정 웹페이지가 읽는 자리**라서 여기에 바로 만듦. ⚠️ 이 폴더가 이미 있으면 지우고 새로 만듦 |
| `--coordinate "[위도,경도,높이]"` | 모델을 지구 위 어디에 놓을지. 3장 출력에 나온 값 그대로 (진주 LH 부지). 높이는 모델 바닥이 지구 표면 살짝 위에 오게 계산된 값 |
| `--no-open-inspector` | 끝나고 자동으로 검사 창을 열지 마라 |
| `--memory-budget 16` | 메모리 16 GB 까지 써도 된다 |

### 4-3. 실행하면 (약 35초)
중간에 표와 로그가 많이 지나갑니다. 마지막에 이렇게 끝나면 성공:
```
[info] nodes=199 | levels=7 logical/16 physical | splats=1660664
[info] total done | ms=32353
[ok] output completed: /home/rocheol2/storage/Cesium/3dgs_measurement_tool/public/models/site01
```
- `splats=1660664`: 원래 255만 개 중 **너무 투명한(불투명도 5 % 미만) 89만 개는 자동으로 뺐습니다.** 대부분 공중에 떠 있는 잡티라 측정엔 오히려 좋습니다. 전부 남기려면 `--opacity-filter 0` 을 붙입니다.

**4-4.** 결과 확인.
```bash
ls 3dgs_measurement_tool/public/models/site01
du -sh 3dgs_measurement_tool/public/models/site01
```
→ `build_summary.json  tiles  tileset.json` 세 개, 크기 약 151 MB.

**4-5.** 방향(위쪽)이 제대로 인식됐는지 확인.
```bash
grep -o '"source_coordinate_system": *"[a-z_]*"' 3dgs_measurement_tool/public/models/site01/build_summary.json
```
→ `"source_coordinate_system": "z_up"` 이면 OK. (`camera_y_down_z_forward` 가 나오면 3장을 거치지 않은 원본 PLY 를 넣은 것 → 모델이 기울어져 보임)

### 4-6. 웹페이지에 모델 이름 등록하기 (Site01 은 ✅ 이미 되어 있음)
웹페이지는 "모델 목록" 을 소스 파일 하나에서 읽습니다: `3dgs_measurement_tool/src/pages/Home.jsx` 의 `BASE_MODELS`. Site01 은 이미 다음이 들어 있습니다.
```js
  {
    id: "Site_Research_01",
    name: "Site_Research_01 (LH drone, local)",
    tilesetUrls: ["/models/site01/tileset.json"],   // public/models/site01/tileset.json 을 뜻함
    offsetHeight: 0,
  },
```
**다른 모델을 추가할 때**: 같은 모양으로 한 덩어리를 더 복사해 넣고, `id`·`name` 을 바꾸고 `tilesetUrls` 의 `site01` 을 새 폴더 이름으로 바꾸면 끝. (VS Code 로 파일을 열어 편집 → 저장하면 웹페이지가 자동 새로고침)
코드를 건드리기 싫으면 웹페이지 안에서 **Model ▸ URL Model ▸ Edit** 에 `http://localhost:5173/models/새폴더/tileset.json` 을 넣어도 됩니다(새로고침하면 다시 입력해야 함).

---

## 5장. 측정 웹페이지 켜기

### 5-1. 켜기
```bash
~/storage/Cesium/tools/start_tool.sh
```
→ 몇 초 뒤 이렇게 보이면 성공:
```
  VITE v7.3.1  ready in 530 ms
  ➜  Local:   http://localhost:5173/
```
- 이 터미널은 **웹페이지가 살아 있는 동안 계속 켜 두어야** 합니다(창을 닫으면 웹페이지도 꺼짐). 다른 명령을 치려면 **터미널을 하나 더** 엽니다(`Ctrl`+`Alt`+`T`).
- 끄고 싶을 때: 이 터미널에서 `Ctrl` + `C`.

### 5-2. 크롬으로 열기
크롬(Google Chrome)을 열고 주소창에 **`http://localhost:5173`** 을 치고 Enter.
(`localhost` = "내 컴퓨터". 인터넷의 어떤 사이트가 아니라 방금 켠 내 컴퓨터의 웹페이지를 보는 것)

### 5-3. 첫 화면에서 보이는 것
- 오른콽 넓은 곳: 파란 지구. 몇 초 뒤 **진주 LH 부지**로 자동으로 날아가고, 그 위에 3D 사진이 나타납니다(처음엔 조각(타일)을 읽느라 10~30초 걸릴 수 있음).
- 왼쪽 패널 위: **Model** 드롭다운 = `Site_Research_01 (LH drone, local)` 이 선택되어 있음.
- 그 아래 **Offset Height**(높이 보정, m): 모델이 땅에 파묻혀 보이면 숫자를 키우고(+5, +10 …), 공중에 떠 있으면 줄입니다. **거리에는 전혀 영향 없음**(그냥 위아래로 옮기는 것).
- 탭 세 개: **Points**(점), **Polylines**(선), **Polygons**(면).

모델이 안 보이면 → 11장 "문제가 생겼을 때".

### 5-4. 마우스로 돌려 보기 (연습 1분)
| 하고 싶은 것 | 마우스 |
|---|---|
| 옆으로 이동 (지구 돌리기) | **왼쪽 버튼** 누른 채 끌기 |
| 확대/축소 | **휠** 굴리기 (또는 오른쪽 버튼 누른 채 위아래로 끌기) |
| **각도 바꾸기(기울이기·빙 돌기)** ← 측정에서 가장 많이 씀 | **가운데 버튼(휠)** 누른 채 끌기, 또는 **`Ctrl` 누른 채 왼쪽 버튼** 끌기 |
| 모델로 다시 돌아가기 | 화면 오른쪽 위 **집 모양(홈) 버튼** |

> 팁: 관심 있는 물체를 화면 **중앙**에 두고 휠로 확대한 뒤, `Ctrl`+왼쪽 드래그로 그 물체를 중심으로 빙 돌면 여러 각도를 쉽게 얻습니다.

---

## 6장. 점 하나 재 보기 (연습)

측정할 점은 **"어디인지 헷갈리지 않는 점"** 이 좋습니다: 건물 모서리, 표지판 끝, 기둥이 땅에 닿는 곳, 창틀 꼭짓점. 둥글거나 밋밋한 면 한가운데는 나쁩니다(어디를 클릭했는지 각도마다 달라짐).

**6-1.** 왼콽 **Points** 탭 → **Start Measure** 버튼 클릭. (이제부터 3D 화면 클릭이 "측정 클릭" 이 됩니다)

**6-2.** 잴 점이 화면에 **크게** 보이도록 휠로 확대합니다. 크게 보일수록 정확합니다.

**6-3.** 그 점을 **정확히 한 번** 클릭. → 화면에 **노란 직선**(광선) 이 하나 생깁니다. (안 생기면 6-9 참고)

**6-4.** `Ctrl` + 왼쪽 드래그로 카메라를 **다른 각도로 확 옮깁니다.** 조금이 아니라 20~40° 이상, 예를 들어 "앞에서 보던 걸 옆에서" 정도로.

**6-5.** 다시 확대해서 **똑같은 점**을 클릭. → 노란 직선 2개.

**6-6.** 6-4 ~ 6-5 를 반복해 **직선이 5개** 되게 합니다(논문도 5개). 각도가 서로 다를수록, 위·옆·반대쪽에서 골고루일수록 좋습니다.

**6-7.** **End Measure** 클릭. → 왼쪽 목록에 점이 추가됩니다:
```
Point 1
(35.17779xxx, 128.14690xxx, 7.1234) | σ = 0.018 m
```
괄호는 (위도, 경도, 높이 m). **σ(시그마)** 는 "5개 직선이 계산된 점에서 평균 몇 m 어긋났나" 입니다.

**6-8. σ 읽는 법**
| σ | 뜻 | 할 일 |
|---|---|---|
| 0.05 m 이하 | 잘 됐음 | 통과 |
| 0.05 ~ 0.2 m | 그럭저럭 | 각도를 더 벌려 다시 하면 좋아짐 |
| 0.3 m 이상 | 클릭 하나가 **다른 점**을 찍었을 가능성 큼 | 목록에서 그 점 선택 → **Delete** → 6-1 부터 다시 |

화면에서는 점 위치에 **파란 타원(오차 타원체)** 이 그려집니다(Error Ellipsoid / Simple Point 토글). 타원이 길쭉하면 그 방향으로 불확실하다는 뜻이니, 그 방향과 직각인 쪽에서 한 번 더 클릭하면 줄어듭니다.

**6-9. 클릭해도 노란 선이 안 생기면**
- Start Measure 를 눌렀는지 확인.
- **하늘을 배경으로 위를 올려다보며** 클릭하면 안 됩니다. 이 도구는 "클릭 방향이 지구 표면과 만나는 점" 을 이용하기 때문에, **약간이라도 내려다보는 각도**에서 클릭해야 합니다. 카메라를 조금 위로 올려 내려다보세요.

---

## 7장. 두 점 사이 거리 구하기

**7-1.** 6장 방법으로 **점 A**(예: 창문 왼쪽 끝)를 재고, 이어서 **점 B**(창문 오른쪽 끝)를 잽니다. 목록에 Point 1, Point 2 가 생깁니다.

**7-2.** Points 탭의 **Export** 버튼 → **As CSV (.csv)**. → 크롬이 `~/Downloads/` 에 파일을 저장합니다(파일명은 `points.csv` 비슷; 크롬 하단/다운로드 목록에서 정확한 이름 확인).

파일 내용은 이런 표입니다(위도·경도·높이·σ):
```
id,lon,lat,alt,accuracy
1,128.14690123,35.17779456,7.1234,0.018
2,128.14701234,35.17779460,7.1301,0.022
```

**7-3.** **새 터미널**(`Ctrl`+`Alt`+`T`)을 열고 계산합니다(이 명령은 conda 방에 안 들어가도 됩니다).
```bash
python3 ~/storage/Cesium/tools/measure_distance.py ~/Downloads/points.csv --pairs 1-2
```
→ 보여야 하는 것:
```
점 2개, 폴리라인 0개 로드 | 축척계수 1.0

  A   -  B   |  거리(m)   | σ_A   σ_B  (도구 σ, m) | √(σA²+σB²)
    1 - 2   |   10.0199 | 0.018 0.022        |  0.028
```
- **거리(m)** = 답. 위도·경도·높이를 지구 중심 좌표(미터)로 바꿔 두 점 사이 직선 길이를 구한 것.
- 마지막 칸은 두 점의 σ 를 합친 **대략의 불확도**.
- 점이 여러 개면 `--all`(모든 쌍) 또는 `--pairs 1-2 3-4`.

**7-4. (선택) 여러 점을 이어서 길이 재기**
Polylines 탭 → **Create Polyline** → 3D 화면에서 측정된 점들을 순서대로 클릭(또는 오른쪽 패널 목록에서 선택 후 Add Selected) → **Finish Polyline** → **Export ▸ CSV** →
```bash
python3 ~/storage/Cesium/tools/measure_distance.py ~/Downloads/polylines.csv --chain
```
→ 구간별 길이와 누적 길이가 나옵니다.

---

## 8장. 진짜 길이와 비교해서 축척 고치기 (정확도를 cm 로)

3장의 GPS 축척은 1~2 % 오차가 있습니다. **현장에서 줌자로 잰 길이가 하나만 있으면** 이 오차를 없앨 수 있습니다.

**8-1.** 길이를 확실히 아는 구간을 정합니다. 예: 줌자로 잰 문 폭 0.90 m, 두 측량 기준점(GCP) 사이 거리, 레이저 스캐너(TLS) 자료에서 잰 길이. 길수록(수 m 이상) 좋습니다.

**8-2.** 6장 방법으로 그 구간 양 끝을 잽니다 → Export CSV (예: Point 1, Point 2, 실측 12.500 m).

**8-3.** 축척계수를 계산합니다.
```bash
python3 ~/storage/Cesium/tools/measure_distance.py ~/Downloads/points.csv --calibrate 1 2 12.500
```
→ 예: `축척계수 s = 1.012345` (= 실측 ÷ 측정). 1 에 가까울수록 GPS 축척이 잘 맞았던 것.

**8-4.** 이후 거리 계산에 이 값을 붙입니다.
```bash
python3 ~/storage/Cesium/tools/measure_distance.py ~/Downloads/points.csv --pairs 3-4 --scale 1.012345
```
(모델 자체를 고치고 싶으면 전문가용 튜토리얼 8단계의 `apply --scale` 방법으로 3~4장을 다시 돌립니다.)

**8-5.** 가능하면 방향이 다른 구간 2~3개로 확인하세요. 계수가 서로 크게 다르면 모델이 한쪽으로 늘어난 것이라 COLMAP 단계에 기준점을 더 넣어야 합니다.

---

## 9장. 다른 모델을 처음부터 (요약 카드)

```bash
conda activate cesium_measure && cd ~/storage/Cesium
mkdir -p data/<이름>
# ③ 미터로
python3 tools/gs_ply_georef.py from-gps --ply <PLY> --images <sparse/0/images.bin> --photos <원본사진폴더> --out data/<이름>/<이름>_enu.ply
# ④ 3D Tiles  (위도,경도,높이 = ③ 출력 마지막 줄에 있는 값)
npx --yes 3dgs-ply-3dtiles-converter@0.6.5 data/<이름>/<이름>_enu.ply 3dgs_measurement_tool/public/models/<이름> --coordinate "[위도,경도,높이]" --no-open-inspector --memory-budget 16
# ④-6 Home.jsx BASE_MODELS 에 항목 추가 (tilesetUrls: ["/models/<이름>/tileset.json"])
# ⑤ tools/start_tool.sh → 크롬 http://localhost:5173 → ⑥⑦ 측정·거리
```

---

## 10장. 자주 하는 질문 (FAQ)

**Q. Cesium(세슘)은 별도 프로그램인가요, 브라우저에서 쓰는 건가요?**
A. **브라우저에서 쓰는 것**입니다. Cesium(정확히는 CesiumJS)은 웹페이지 안에서 3D 지구를 그려 주는 자바스크립트 부품이고, 논문 도구 웹페이지 안에 이미 들어 있습니다. 우리가 켜는 것은 그 웹페이지를 내 컴퓨터에서 서비스하는 작은 서버(`start_tool.sh`)이고, **크롬으로 `http://localhost:5173` 에 접속**해서 씁니다. 설치할 "Cesium 프로그램" 은 없습니다.
(참고: "Cesium ion" 이라는 같은 회사의 **클라우드 서비스**가 따로 있는데, 데이터를 올려 변환·보관해 주는 곳입니다. 이 튜토리얼은 그것을 쓰지 않습니다. 배경 지도 사진만 도구에 들어 있는 저자들의 ion 계정 열쇠(토큰)로 받아옵니다.)

**Q. 3D Tiles 변환은 파이썬 코드인가요, 세슘 프로그램인가요? 어디서 받나요?**
A. **둘 다 아닙니다.** `3dgs-ply-3dtiles-converter` 라는 **Node.js 로 만든 오픈소스 명령줄 도구**입니다(npm 이라는 자바스크립트 부품 가게에 올라와 있음). **다운로드는 `npx` 명령이 처음 실행할 때 자동으로** 합니다(이미 받아져 있음). 파이썬은 제가 만든 두 스크립트(축척 맞추기 `gs_ply_georef.py`, 거리 계산 `measure_distance.py`)에만 쓰입니다.

**Q. 그럼 내가 직접 설치해야 하는 게 있나요?**
A. 없습니다. Node.js(conda 방 `cesium_measure`), 논문 도구, 변환기 모두 2026-09-05 에 준비했습니다. 다른 PC 에서 처음부터 하려면 저장소의 `setup.sh` 하나를 실행하면 됩니다.

**Q. 왜 측정 전에 축척을 맞춰야 하나요? 도구가 알아서 못 하나요?**
A. 도구는 "미터 단위 지구 좌표" 만 다룹니다. 모델이 COLMAP 임의 단위면 도구는 그 숫자를 미터로 착각합니다(Site01 이면 실제 거리의 1/4.2 가 나옴). 그래서 3장이 필요합니다.

**Q. 3장에서 나온 "잔차 RMS 0.35 m" 면 측정도 35 cm 틀리나요?**
A. 아닙니다. 35 cm 는 **드론 GPS 의 흔들림**이고, 191장 평균으로 축척을 구하므로 축척 오차는 1~2 % 로 줄어듭니다. 점 자체의 정밀도는 클릭 정확도(σ, 보통 몇 cm)입니다. 거리 오차 ≈ 클릭 오차 + (거리 × 1~2 %). 8장을 하면 두 번째 항이 사라집니다.

**Q. 왜 같은 점을 5번이나 클릭하나요? 2번이면 안 되나요?**
A. 2번이면 계산은 되지만 둘이 어긋났을 때 **누가 틀렸는지** 모릅니다. 5번이면 하나가 튀어도 σ 가 커져서 바로 알 수 있고, 평균이 잡혀 더 정확합니다(논문도 5회).

**Q. Offset Height 를 바꾸면 거리가 달라지나요?**
A. 아닙니다. 모델 전체를 위아래로 옮기기만 하므로 두 점 사이 거리는 그대로입니다.

**Q. 크롬 대신 파이어폭스도 되나요?**
A. WebGL2 를 지원하면 됩니다. 크롬에서 검증했으니 크롬을 권합니다.

**Q. 결과 좌표(위도·경도)는 진짜 위치인가요?**
A. 대략(수 m 이내)입니다. GPS 평균으로 놓았기 때문입니다. **거리는 정확**하지만, 절대 위치(측량 좌표)가 필요하면 GCP 로 맞춘 DS003 방식이 필요합니다.

---

## 11장. 문제가 생겼을 때 (위에서부터 확인)

1. **터미널에 `(cesium_measure)` 가 없다** → `conda activate cesium_measure`.
2. **`npm: command not found` / `npx: command not found`** → 1번과 같음.
3. **크롬에 "연결할 수 없음"** → 5-1 의 터미널이 켜져 있고 `Local: http://localhost:5173/` 가 떠 있는지 확인. 주소를 `http://localhost:5173` 정확히.
4. **지구는 보이는데 모델이 없다** → (a) 20~30초 기다림, (b) 홈 버튼, (c) Offset Height 를 +10, +20 … 로 올려 봄(땅에 묻힌 경우), (d) `F12` → Console 탭에 `Error loading tileset` 이 있으면 `public/models/site01/tileset.json` 이 있는지 4-4 로 확인.
5. **모델이 기울어져 있거나 뒤집혀 있다** → 3장을 거치지 않은 PLY 를 변환한 것. 4-5 로 확인 후 3장부터 다시.
6. **화면이 매우 느리다** → 도구가 자동으로 화질을 낮춰 재시도합니다(콘솔 로그). 계속 느리면 4장에서 `--sh 0` 을 붙여 다시 변환(용량 대폭 감소, 색이 약간 단순해짐).
7. **클릭해도 노란 선이 안 생김** → 6-9.
8. **σ 가 크다** → 6-8 표.
9. **변환기가 메모리 오류** → `--memory-budget 8` 로 낮추거나 `simplify --target-count 1000000` 로 먼저 줄임(전문가용 튜토리얼 3단계).
10. **2DGS 파일이 "scale_2 없음" 오류** → 2-1 의 `point_cloud_3dgs.ply` 사용.

그래도 안 되면 전문가용 [3DGS_거리측정_튜토리얼.md](3DGS_거리측정_튜토리얼.md) 의 10장, 그리고 논문 도구의 사용 설명서 `3dgs_measurement_tool/docs/OPERATION_GUIDE.md` 를 보세요.
