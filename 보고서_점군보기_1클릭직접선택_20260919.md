# 작업 결과 보고서 — 점군 보기 · 1클릭 직접 선택 · 정밀화

작성일 2026-09-19 · 대상 `webapp/` (HTML 뷰어) · 반영 커밋 ebfdf6c · 온라인 https://rocheol2.github.io/3DGS_Accurate_Point_Mesurement/

## 1. 요청 내용

1. 3DGS 생성물(PLY)을 CloudCompare에서 여는 것처럼 **점군 형태로도 볼 수 있게** 하고, Tab 키나 버튼으로 스플랫 화면과 바로 전환.
2. 점군에서 **점을 바로 선택**해 거리 측정 등에 쓰게 하여 여러 번 클릭하는 수고를 줄임.
3. 1클릭 선택 시 좌표 결정 방식은 **"앞쪽 군집 중앙값"을 기본**으로 하고 "가장 가까운 점 하나"를 옵션으로 두는 권장안 채택.
4. 사용자가 쓰면서 **두 방식의 차이를 알 수 있는 설명 장치**를 함께 반영.

## 2. 설계 결정

| 결정 | 이유 |
|---|---|
| 점군은 별도 파일 변환 없이 이미 읽은 PLY 버퍼에서 바로 생성 | 위치·색(SH 0차)·불투명도가 파싱 단계에 있으므로 추가 비용이 거의 없음. 큰 좌표 원점 이동·위 방향·축척 처리도 그대로 공유 |
| 1클릭 선택의 기본을 군집 중앙값으로 | 가우시안 중심은 표면 근처에 스플랫 두께만큼 흩어져 있고 잡티가 떠 있어, 한 점을 그대로 쓰면 오차가 무작위로 생김. 군집 중앙값은 흩어짐을 평균하고 외톨이를 무시하며 σ를 제공 |
| "가장 가까운 점 하나"를 옵션으로 유지 | 기둥 끝·모서리 같은 뾰족한 특징점에서는 한 점이 더 정확할 수 있음 |
| 직접 선택 점을 별도 배지·방법 값으로 구분하고 [정밀화] 제공 | 이 방식은 논문이 다시점 방법보다 부정확하다고 지적한 "점군 직접 찍기"에 해당. 빠른 측정으로 쓰되 정밀도가 필요하면 같은 점을 다시점 결과로 교체할 수 있어야 함 |
| Shift+클릭은 항상 다시점 광선 | 점군 화면에서도 정밀 측정을 즉시 선택할 수 있게 |

## 3. 구현 내용

### 3.1 점군 보기
- `buildCloudFromPly`: PLY 위치(x,y,z), 색(0.5 + 0.2821·f_dc), 불투명도(sigmoid < 0.05 제외)로 `Float32Array`/`Uint8Array` 생성. 압축 PLY·SPZ 등은 렌더러(Spark)의 `forEachSplat`으로 추출(`buildCloudFromMesh`).
- `THREE.Points` + `PointsMaterial`(화면 픽셀 크기 고정). 보기 모드 `splat` / `cloud` / `both`(도구막대 [☁ 보기] 메뉴, **Tab** 전환), 점 크기 1~6 px, 색 모드 RGB / 높이(위 축 기준 2~98 % 구간 색상 매핑) / 단색.
- 휠 줌의 표면 추정 표본(`state.centers`)도 이 점군에서 최대 20만 점으로 추출.

### 3.2 1클릭 직접 선택 (`directPick`)
- 클릭 광선 주변 원뿔(반경 설정 3~20 px, 기본 8; 점이 없으면 2배씩 40 px까지 확대) 안의 중심점을 **전체 점군에서** 탐색. 4.5만~수백만 점 브루트포스로 클릭당 수십 ms.
- **cluster(기본)**: 거리순 정렬 → 12 % 깊이 띠 안에 K점(3 또는 전체의 3 %) 이상 모인 첫 무리를 표면으로 택해 좌표 중앙값 사용, σ = 무리의 3D 퍼짐(RMS/√3).
- **nearest**: 커서에 각도상 가장 가까운 한 점, σ 0(불확도 미상).
- 점 객체에 `method: 'pick'`, `pickMode`, 품질 `'pick'`(배지 **직접선택**). 공분산은 σ²·I.
- 라우팅(`onMeasureClick`): 점군/겹침 보기이거나 설정 "스플랫에서도 1클릭"이면 첫 클릭이 직접 선택. Shift 클릭(`state.forceRay`)은 다시점 광선. 분석 작업에서 기존 마커 클릭은 그대로 재사용.

### 3.3 정밀화 (`startRefine`)
결과 표의 [정밀화] → 그 점을 피벗으로 다시점 점 측정을 시작(자동 회전도 그 점 중심). 완료 시 같은 점(id·이름 유지)의 좌표·공분산·품질·방법을 교체하고, 이를 참조하는 거리·분석 항목은 자동 갱신.

### 3.4 설명 장치 (요청 4)
1. 설정 탭 "점군 보기 · 1클릭 직접 선택"에서 두 선택지를 **카드형 라디오**로 두고 각각의 동작·장단점 설명문 표시.
2. 그 옆 **[? 설명]** 버튼 → 모달: 왜 결과가 달라지는지(가우시안의 두께·잡티), 상황별 비교표(평면 / 잡티 / 뾰족한 특징점 / 재현성 / σ), 권장 사용법, 논문의 지적, Shift 안내.
3. **처음 직접 선택을 실행할 때** 위 모달을 한 번 자동 표시(설정 `pickHelpSeen`에 기록).
4. 결과 표: 배지 툴팁(품질 기준 설명)과 하단 범례("직접선택 = 점군 1클릭, σ는 군집 퍼짐, [정밀화] 권장").
5. 코치 바: 현재 방식("앞쪽 군집 중앙값" / "가장 가까운 점 하나")과 "직접선택 = 빠르지만 스플랫 두께만큼 불확실", Shift 안내.
6. 도움말(help.html)·README·사용법 문서에 비교표.
- 부수: CSV `method` 열(`multi_ray` / `direct_pick_cluster` / `direct_pick_nearest`), [정보 점검]에 점군 항목(점 수·보기·방식·원뿔 반경).

## 4. 검증 (헤드리스 Chrome, 2.000 m 정육면체 45,120 가우시안)

| 항목 | 결과 |
|---|---|
| 점군 생성·Tab 전환 | 45,120점, 전환 후 스플랫 숨김·점군 표시, 화면 표본 3/3 렌더 확인 |
| 군집 중앙값 선택 (꼭짓점) | 오차 **0.2 mm**, 군집 77점, σ 13.7 mm |
| 가장 가까운 점 하나 | 오차 4.9 mm, σ 0 |
| 꼭짓점 앞 0.5 m에 잡티 2점 삽입 | 군집 방식 오차 0.0 mm (외톨이 무시 확인) |
| 설명 모달 첫 사용 시 자동 표시 | 확인 |
| Shift+클릭 | 다시점 광선 1개 생성 |
| 정밀화 | 5회 클릭 후 같은 점이 `multi`/좋음, 오차 0.0 mm, 점 개수 유지 |
| 기존 회귀(다시점 측정·거리)·분석(각도·길이·면적) | 통과 (http, file://) |

스크린샷: `webapp/docs/shots/28_cloud_view.png`(점군), `29_pick_help.png`(설명 창), `30_pick_results.png`(배지·정밀화). 테스트: `webapp/tests/test_cloud.js`.

## 5. 한계와 유의점

- 직접 선택의 정확도는 스플랫 두께(수 cm~수십 cm)와 잡티에 좌우됩니다. σ와 배지를 보고 필요하면 [정밀화]를 쓰는 것이 전제입니다.
- 브루트포스 탐색이라 수백만 점에서는 클릭당 수십~수백 ms가 걸릴 수 있습니다(문제 시 공간 격자 색인으로 개선 가능).
- 점군 색은 SH 0차(방향 무관 평균색)만 사용해 스플랫보다 단조롭습니다.
- 헤드리스 검증은 소프트웨어 렌더러 기준이며, 실제 GPU에서의 큰 점군 표시 성능은 사용자 환경에서 확인이 필요합니다.

## 6. 사용법 요약

1. 파일을 연 뒤 **Tab** → 점군 화면. [● 점 측정]/[↔ 거리 측정]/[📐 분석] 후 점군을 **한 번 클릭**하면 점이 정해짐.
2. 처음 한 번 뜨는 설명 창에서 두 방식의 차이를 확인. 바꾸려면 설정 탭의 카드 선택 또는 [? 설명] → [설정에서 방식 바꾸기].
3. 정밀도가 필요한 점은 결과 표의 **[정밀화]**, 또는 처음부터 **Shift+클릭**.

## 7. 다음 단계 제안

2단계 분석(점–면 거리, 평면 적합·평탄도) → 3단계(프리즘형 체적, 단면). 필요 시 대용량 점군용 공간 격자 색인과 점군 색상에 SH 1차 반영.
