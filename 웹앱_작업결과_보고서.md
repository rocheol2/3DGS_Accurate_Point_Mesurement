# 작업 결과 보고서 — 3DGS 다시점 거리 측정기 (설치 없는 웹앱)

작성일 2026-09-05 · 저장소 `rocheol2/3DGS_Accurate_Point_Mesurement` · 앱 폴더 `webapp/`

## 1. 목적

Deng & Qin (2026) *Accurate Point Measurement in 3DGS* 가 제안한 **다시점 광선 최소제곱 교회** 방법을, 일반 사용자가 **별도 프로그램·변환·계정 없이** 브라우저에서 바로 쓸 수 있게 만드는 것. 요구 사항:

1. 측정에 필요한 정보가 없으면 **무엇이 없어서 안 되는지** 정확하고 확실하게 알릴 것 (없는 정보를 만들어 내지 않음).
2. 여러 각도에서 같은 점을 찍어야 하므로 **안내선과 확대창** 중심의 사용자 경험.
3. 직관적 사용법, 사용 설명 자료, 웹에서 구동되는 기능 소개, 작업 보고서.

## 2. 설계 결정

| 결정 | 이유 |
|---|---|
| Cesium·3D Tiles 를 쓰지 않고 **3DGS 파일을 브라우저에서 직접 렌더링** | 논문 방법에 필요한 것은 "카메라 위치 + 클릭 픽셀의 광선"뿐. 지구·위경도·3D Tiles 변환·Node 서버는 저자들의 지리공간 맥락에서 온 짐이라 제거. 이로써 설치·변환·계정이 모두 사라짐 |
| 렌더러 **Spark 2.1.0 + three.js 0.180.0** (MIT) | PLY(원본·SuperSplat 압축)·SPZ·SPLAT·KSPLAT·SOG 를 모두 읽음. 순수 three.js 구조라 픽셀 읽기(확대창·템플릿 매칭)와 카메라 제어를 직접 다룰 수 있음. 라이브러리는 `webapp/vendor/` 에 포함(vendoring)해 CDN 없이 동작 |
| 정적 파일만 사용, 서버 로직 없음 | GitHub Pages 등 어디서나 호스팅. 파일은 사용자 PC 를 떠나지 않음(개인정보·보안) |
| 오류 메시지를 **단일 카탈로그**(`errors.js`, E01~E13)로 관리 | 앱 토스트·정보 점검표·도움말 표가 같은 정의를 사용 → 문구 불일치 방지. 각 항목 = 제목 / 왜 안 되나 / 해결 방법 |
| 축척 상태를 **명시적 상태값**으로 관리 | 알려짐(출처 기록) / 미확인 → 미확인이면 배너·⚠·CSV 출처 열까지 일관되게 "모델 단위" 표기 |

## 3. 구현 내용

### 3.1 파일 구성 (`webapp/`)

| 파일 | 역할 | 크기 |
|---|---|---|
| `index.html` | 레이아웃(도구막대·3D 화면·오버레이·확대창·오른쪽 패널·코치 바·시작 화면·모달), 임포트맵 | 175줄 |
| `app.js` | 파일 해석·렌더링·측정 로직·UI 전체 | 640줄 |
| `errors.js` | 오류·경고 카탈로그 E01~E13 + 품질 기준 | 공유 모듈 |
| `tour.js` | 첫 방문 기능 소개 투어(스포트라이트 + SVG 삽화 6단계) | |
| `help.html` | 웹 사용 설명서(따라하기·안내선 설명·축척·결과 읽기·오류 전체 표 자동 생성·FAQ) | |
| `style.css` | 다크 테마, 반응형 | |
| `vendor/` | three.module.js, three.core.js, OrbitControls.js, Pass.js, spark.module.js, 라이선스 | 7.2 MB |
| `sample/` | 진주 LH 현장 샘플(15만 가우시안·SH0·미터·Z-up) + 사이드카 JSON | 9.8 MB |
| `docs/shots/` | 자동 테스트 스크린샷 9장 | |

### 3.2 측정 파이프라인 (논문과의 대응)

1. 클릭 → three.js `Raycaster` 로 카메라 위치 Cᵢ 와 단위방향 dᵢ 획득(파일 좌표계).
2. N ≥ 2 광선 → Σ(I − dᵢdᵢᵀ) x = Σ(I − dᵢdᵢᵀ) Cᵢ 를 풀어 x 계산 (논문 식 (6) 의 정규방정식과 동치).
3. 잔차 vᵢ = (I − dᵢdᵢᵀ)(x − Cᵢ), σ̂₀² = Σ‖vᵢ‖²/(2N − 3) (식 (7)), 공분산 Q = σ̂₀²[Σ(I − ddᵀ)]⁻¹ (식 (8)).
4. 추가 지표: 각 광선 잔차를 픽셀로 환산(‖vᵢ‖/깊이 × 초점거리[px]), 광선 간 최대 각도 → 품질 좋음/보통/나쁨.
5. 거리 d = ‖x_B − x_A‖, σ_d = √(uᵀ(Q_A + Q_B)u) (u = 방향 단위벡터) + 축척 불확도 항.

### 3.3 사용자 경험 기능

- **안내선(에피폴라 선)**: 첫 광선을 현재 시점에 투영해 노란 점선으로 표시. 2번째 클릭은 30 px 이내면 선 위로 스냅.
- **확대창(루페)**: 커서 주변 2~8× 확대, 안내선·현재 추정점·십자선을 확대창 안에도 그림. `[` `]` 로 배율.
- **자동 정밀 보정**: 첫 클릭 주변 25×25 px 무늬를 기억하고 이후 클릭을 정규화 상관(NCC ≥ 0.6, 사용자 클릭 대비 +0.05 이상 개선 시)으로 다듬음. 2번째 클릭은 안내선 방향 ±40 px, 3번째 이후는 ±8 px 만 허용(보수적). 보정량·유사도를 광선 표에 표시. 논문이 "향후 과제"로 적은 반자동 대응점 탐색의 경량 구현.
- **자동 회전**: 현재 추정점을 중심으로 35°(+상하 12° 교대) 회전 애니메이션.
- **각도 막대·코치 바**: 첫 광선과 현재 시선의 각도를 실시간 표시, 하단 코치 바가 "지금 할 일"을 단계(k/5)와 함께 안내.
- **첫 클릭 후 자동 정렬**: 클릭한 점을 궤도 중심으로 부드럽게 이동 → 이후 회전이 그 점을 중심으로 이루어짐.
- **결과 패널**: 점 표(좌표·σ·N·품질 배지·삭제), 두 점 체크 → 거리, 거리 표(± 포함), 화면 라벨.
- **축척 보정 마법사** 3단계(참값 입력 → 양 끝점 측정 → 계수 적용/사이드카 JSON 저장), **축척 직접 입력**, 파일별 localStorage 기억.
- **정보 점검 모달**: WebGL2·파일·3DGS 속성·축척·위 방향·진행 중 측정(광선 수/각도/잔차)·결과를 ✓/!/✕ 로 표시, 각 실패 항목에 해결 방법.
- **내보내기**: CSV(u·m 좌표, 축척 출처), JSON(공분산·광선), PNG.
- **기능 소개 투어**: 첫 방문 자동 실행(6단계, 광선 교회·안내선 SVG 삽화), `?` 도움말에서 재실행.

### 3.4 "없는 정보" 처리 원칙

| 부족한 정보 | 앱의 동작 |
|---|---|
| 축척 | 배너 E05 + 토스트: "미터 거리를 표시할 수 없다" 는 사실, 이유(COLMAP 임의 단위), 해결 3가지. 거리·좌표에 `u` 단위와 ⚠, CSV 미터 열 공란·출처 열에 명시. 보정·직접 입력·사이드카로 해결되면 출처를 항상 함께 표시 |
| 광선 부족(<2) | E07 진행 불가 |
| 광선 각도 부족(<10°) | E08 확정 차단 + [그래도 확정(비권장)] / [마지막 광선 취소] 선택 제공 |
| 잔차 과대 | E09 경고, 점은 만들되 품질 "나쁨" 표시 |
| 위 방향 | E06 안내(측정 무관)와 기본 가정 명시 |
| 형식·속성 오류 | E02/E03/E04 에서 무엇이 들어 있는지(속성 목록)와 올바른 파일 안내 |

## 4. 검증

### 4.1 자동 테스트 (헤드리스 Chrome + SwiftShader, `puppeteer-core`)

합성 3DGS PLY: 한 변 **2.000 m** 정육면체 와이어프레임(45,120 가우시안, 꼭짓점 노란 마커, 회색 바닥). 스크립트가 카메라를 5개 시점에 놓고 꼭짓점의 투영 픽셀(±0.5 px 난수)을 클릭.

| 항목 | 결과 |
|---|---|
| 렌더링 ↔ 계산 좌표계 일치 | 투영 픽셀의 실제 색 = 노란 마커(255,255,51) → 렌더 프레임이 파일 좌표와 동일함을 확인 |
| 꼭짓점 (2,2,2) 5시점 측정 | 3D 오차 **2.5 mm**, σ₀ 1.3 mm, 잔차 RMS 0.25 px, 최대각 50°, 품질 좋음 |
| 꼭짓점 (0,2,2) 5시점 측정 | 3D 오차 **3.6 mm** |
| 두 꼭짓점 거리 (참값 2.000 m) | **2.0019 ± 0.0036 m** |
| 축척 없는 파일(`cube.ply`) | 배너 E05 표시, 단위 "모델 단위(u)", 토스트 E05·E06 |
| 같은 시점 2회 클릭 후 확정 | E08 차단(최대 각도 0.8°) + 대안 버튼 |
| 축척 직접 입력 | 단위 표시가 "미터 (m)" 로 전환 |
| 콘솔 오류 | 없음 |

실제 3DGS 샘플(진주 LH, 15만 가우시안): 로드 1.3 s(소프트웨어 렌더링 기준), PLY 헤더의 `units: meters`, `up axis: z` 자동 인식.

스크린샷: `webapp/docs/shots/` (01 시작 화면, 02 안내선, 03 거리 표시, 04 정보 점검, 05 축척 없음 배너, 06 E08, 07~09 샘플 렌더·확대창·자동 회전).

### 4.2 테스트 중 발견·수정한 문제

- `hidden` 속성 요소(모달·로딩·배너)가 CSS `display:flex` 에 덮여 항상 보임 → `[hidden]{display:none!important}` 추가.
- 시작 화면이 상단 도구막대를 가려 버튼 클릭 불가 → 도구막대를 시작 화면 위로 올림.
- 자동 정밀 보정이 시점 차가 큰 경우 1~2 px 편향 유발 가능 → 유사도 개선 여유 0.05, 3번째 이후 이동 8 px 제한으로 보수화.
- 초기 프레이밍이 멀리 떠 있는 배경 가우시안에 휘둘림 → 거리 75 퍼센타일 사용.

### 4.3 검증하지 못한 것

- 실제 GPU·실제 사람의 마우스 조작(헤드리스 소프트웨어 렌더링과 프로그램 클릭으로 대체). 확대창·투어의 시각적 배치는 스크린샷으로만 확인.
- 대용량(250만 가우시안, 600 MB) PLY 의 브라우저 메모리·속도: 데스크톱 Chrome 에서 가능할 것으로 예상하나 미측정. SPZ/SPLAT/KSPLAT/SOG 실제 파일 로드는 Spark 문서 기준이며 직접 시험하지 않음.

## 4.4 사용자 보고 문제와 수정 (2026-09-05 저녁)

**증상**: 버튼(파일 열기·파일 선택·샘플)이 반응하지 않고, PLY 를 끌어다 놓으면 파일이 새 탭에 텍스트로 열림.
**원인**: `webapp/index.html` 을 더블클릭해 `file://` 로 연 것. Chrome 은 `file://` 원본에서 외부 모듈 스크립트(`app.js`) 로드를 CORS 정책으로 차단한다(헤드리스 재현: "Cross origin requests are only supported for protocol schemes: http, https…"). 스크립트가 전혀 실행되지 않아 드롭 핸들러도 없었고, 브라우저 기본 동작으로 파일이 열렸다. GitHub Pages 도 아직 켜져 있지 않아(404) 온라인 경로도 없었다.

**수정**
1. **단일 파일 버전** `webapp/dist/3DGS_거리측정기_단일파일.html` (6.8 MB): esbuild 로 three·Spark·앱 코드를 한 번들로 묶어 CSS 와 함께 HTML 에 내장(`webapp/build_single_file.mjs`). Spark 는 워커를 Blob URL 로 만들어 번들 안에서도 동작함을 확인. `file://` 로 열어 전체 E2E 통과.
2. `index.html` 을 `file://` 로 열면 인라인 스크립트가 원인과 세 가지 해결 방법(단일 파일·로컬 서버 스크립트·온라인)을 화면에 표시하고, 드롭 시 파일로 이동하는 기본 동작을 차단.
3. **실행 스크립트** `tools/start_webapp.sh`: 로컬 서버를 띄우고 브라우저를 자동으로 연다.
4. `help.html` 의 오류 표를 빌드 시 정적 HTML 로 굽기(모듈 스크립트 제거) → `file://` 에서도 표시.
5. (빌드 과정 버그) 번들을 HTML 에 넣을 때 `String.replace` 의 문자열 치환이 `$$` 를 `$` 로 해석해 `$$` 헬퍼가 `$` 를 덮어쓰는 문제 발견 → 함수 치환으로 수정 후 `file://` 전체 E2E 통과(3D 오차 4.4~5.2 mm, 거리 2.0028 ± 0.0026 m).
6. 큰 파일 경고(E13) 기준을 300만 가우시안 또는 700 MB 로 낮추고 예상 메모리·해결 명령을 표시. 사용자가 열려던 DS003 PLY(1.4 GB, 586만 가우시안)는 GCP 축척 9.7429 를 적용해 미터 단위·150만 가우시안·SH0 으로 축소한 브라우저용 파일을 `data/ds003/` 에 준비.

## 5. 한계와 향후 과제

- 축척은 여전히 외부 정보(실측 길이·GCP·GPS)가 필요합니다. 앱은 "쉽게 한 번" 넣게 할 뿐입니다.
- 결과 좌표는 파일 좌표계입니다. 절대 좌표가 필요하면 `tools/gs_ply_georef.py` 로 지오리퍼런싱 후 열어야 합니다.
- 자동 정밀 보정은 시점 차가 크면(무늬 변형) 동작하지 않도록 설계되어, 큰 각도에서는 사람의 클릭에 의존합니다. 향후: 아핀 보정 템플릿 매칭, 다중 스케일 매칭.
- 향후: 면적·체적, 여러 파일 비교, 드론 사진+COLMAP 으로 브라우저 안에서 축척 자동 추정(현재 파이썬 스크립트 기능의 JS 이식).

## 6. 배포 방법

1. 저장소를 GitHub 에 푸시(완료).
2. GitHub → Settings → Pages → Deploy from a branch → `main` / `(root)` → Save.
3. `https://rocheol2.github.io/3DGS_Accurate_Point_Mesurement/` → 루트 `index.html` 이 `webapp/` 으로 자동 이동. `.nojekyll` 로 Jekyll 처리 비활성.
4. 로컬: `python3 -m http.server 8000` 후 `http://localhost:8000/webapp/`.

## 7. 사용 라이브러리·라이선스

- Spark 2.1.0 (World Labs, MIT) — `webapp/vendor/LICENSE-spark.txt`
- three.js 0.180.0 (MIT) — `webapp/vendor/LICENSE-three.txt`
- 테스트: puppeteer-core(개발용, 저장소 미포함), Google Chrome headless

## 8. 산출물 목록

| 경로 | 내용 |
|---|---|
| `webapp/` | 웹앱 전체(정적) · `dist/3DGS_거리측정기_단일파일.html` 더블클릭용 · `build_single_file.mjs` 빌드 스크립트 |
| `tools/start_webapp.sh` | 로컬 서버 + 브라우저 자동 실행 |
| `webapp/help.html` | 웹 사용 설명서 |
| `웹앱_사용법.md` | 사용법(마크다운) |
| `웹앱_작업결과_보고서.md` | 이 문서 |
| `index.html`, `.nojekyll` | GitHub Pages 진입 |
| `webapp/docs/shots/*.png` | 검증 스크린샷 |
