# 작업 결과 보고서 — 3DGS 다시점 거리 측정기 (설치 없는 웹앱)

작성일 2026-09-05 · 저장소 `rocheol2/3DGS_Accurate_Point_Mesurement` · 앱 폴더 `webapp/`

## 1. 목적

Deng & Qin (2026) *Accurate Point Measurement in 3DGS* 가 제안한 **다시점 광선 최소제곱 교회** 방법을, 일반 사용자가 **별도 프로그램·변환·계정 없이** 브라우저에서 바로 쓸 수 있게 만드는 것. 요구 사항:

1. 측정에 필요한 정보가 없으면 **무엇이 없어서 안 되는지** 정확하고 확실하게 알릴 것 (없는 정보를 만들어 내지 않음).
2. 여러 각도에서 같은 점을 찍어야 하므로 **안내선과 확대창** 중심의 사용자 경험.
3. 직관적 사용법, 사용 설명 자료, 웹에서 구동되는 기능 소개, 작업 보고서.

## 2. 설계 결정

| 결정 | 이유 |
|---|---|
| Cesium·3D Tiles 를 쓰지 않고 **3DGS 파일을 브라우저에서 직접 렌더링** | 논문 방법에 필요한 것은 "카메라 위치 + 클릭 픽셀의 광선"뿐. 지구·위경도·3D Tiles 변환·Node 서버는 저자들의 지리공간 맥락에서 온 짐이라 제거. 이로써 설치·변환·계정이 모두 사라짐 |
| 렌더러 **Spark 2.1.0 + three.js 0.180.0** (MIT) | PLY(원본·SuperSplat 압축)·SPZ·SPLAT·KSPLAT·SOG 를 모두 읽음. 순수 three.js 구조라 픽셀 읽기(확대창·템플릿 매칭)와 카메라 제어를 직접 다룰 수 있음. 라이브러리는 `webapp/vendor/` 에 포함(vendoring)해 CDN 없이 동작 |
| 정적 파일만 사용, 서버 로직 없음 | GitHub Pages 등 어디서나 호스팅. 파일은 사용자 PC 를 떠나지 않음(개인정보·보안) |
| 오류 메시지를 **단일 카탈로그**(`errors.js`, E01~E13)로 관리 | 앱 토스트·정보 점검표·도움말 표가 같은 정의를 사용 → 문구 불일치 방지. 각 항목 = 제목 / 왜 안 되나 / 해결 방법 |
| 축척 상태를 **명시적 상태값**으로 관리 | 알려짐(출처 기록) / 미확인 → 미확인이면 배너·⚠·CSV 출처 열까지 일관되게 "모델 단위" 표기 |

## 3. 구현 내용

### 3.1 파일 구성 (`webapp/`)

| 파일 | 역할 | 크기 |
|---|---|---|
| `index.html` | 레이아웃(도구막대·3D 화면·오버레이·확대창·오른쪽 패널·코치 바·시작 화면·모달), 임포트맵 | 175줄 |
| `app.js` | 파일 해석·렌더링·측정 로직·UI 전체 | 640줄 |
| `errors.js` | 오류·경고 카탈로그 E01~E13 + 품질 기준 | 공유 모듈 |
| `tour.js` | 첫 방문 기능 소개 투어(스포트라이트 + SVG 삽화 6단계) | |
| `help.html` | 웹 사용 설명서(따라하기·안내선 설명·축척·결과 읽기·오류 전체 표 자동 생성·FAQ) | |
| `style.css` | 다크 테마, 반응형 | |
| `vendor/` | three.module.js, three.core.js, OrbitControls.js, Pass.js, spark.module.js, 라이선스 | 7.2 MB |
| `sample/` | 진주 LH 현장 샘플(15만 가우시안·SH0·미터·Z-up) + 사이드카 JSON | 9.8 MB |
| `docs/shots/` | 자동 테스트 스크린샷 9장 | |

### 3.2 측정 파이프라인 (논문과의 대응)

1. 클릭 → three.js `Raycaster` 로 카메라 위치 Cᵢ 와 단위방향 dᵢ 획득(파일 좌표계).
2. N ≥ 2 광선 → Σ(I − dᵢdᵢᵀ) x = Σ(I − dᵢdᵢᵀ) Cᵢ 를 풀어 x 계산 (논문 식 (6) 의 정규방정식과 동치).
3. 잔차 vᵢ = (I − dᵢdᵢᵀ)(x − Cᵢ), σ̂₀² = Σ‖vᵢ‖²/(2N − 3) (식 (7)), 공분산 Q = σ̂₀²[Σ(I − ddᵀ)]⁻¹ (식 (8)).
4. 추가 지표: 각 광선 잔차를 픽셀로 환산(‖vᵢ‖/깊이 × 초점거리[px]), 광선 간 최대 각도 → 품질 좋음/보통/나쁨.
5. 거리 d = ‖x_B − x_A‖, σ_d = √(uᵀ(Q_A + Q_B)u) (u = 방향 단위벡터) + 축척 불확도 항.

### 3.3 사용자 경험 기능

- **안내선(에피폴라 선)**: 첫 광선을 현재 시점에 투영해 노란 점선으로 표시. 2번째 클릭은 30 px 이내면 선 위로 스냅.
- **확대창(루페)**: 커서 주변 2~8× 확대, 안내선·현재 추정점·십자선을 확대창 안에도 그림. `[` `]` 로 배율.
- **자동 정밀 보정**: 첫 클릭 주변 25×25 px 무늬를 기억하고 이후 클릭을 정규화 상관(NCC ≥ 0.6, 사용자 클릭 대비 +0.05 이상 개선 시)으로 다듬음. 2번째 클릭은 안내선 방향 ±40 px, 3번째 이후는 ±8 px 만 허용(보수적). 보정량·유사도를 광선 표에 표시. 논문이 "향후 과제"로 적은 반자동 대응점 탐색의 경량 구현.
- **자동 회전**: 현재 추정점을 중심으로 35°(+상하 12° 교대) 회전 애니메이션.
- **각도 막대·코치 바**: 첫 광선과 현재 시선의 각도를 실시간 표시, 하단 코치 바가 "지금 할 일"을 단계(k/5)와 함께 안내.
- **첫 클릭 후 자동 정렬**: 클릭한 점을 궤도 중심으로 부드럽게 이동 → 이후 회전이 그 점을 중심으로 이루어짐.
- **결과 패널**: 점 표(좌표·σ·N·품질 배지·삭제), 두 점 체크 → 거리, 거리 표(± 포함), 화면 라벨.
- **축척 보정 마법사** 3단계(참값 입력 → 양 끝점 측정 → 계수 적용/사이드카 JSON 저장), **축척 직접 입력**, 파일별 localStorage 기억.
- **정보 점검 모달**: WebGL2·파일·3DGS 속성·축척·위 방향·진행 중 측정(광선 수/각도/잔차)·결과를 ✓/!/✕ 로 표시, 각 실패 항목에 해결 방법.
- **내보내기**: CSV(u·m 좌표, 축척 출처), JSON(공분산·광선), PNG.
- **기능 소개 투어**: 첫 방문 자동 실행(6단계, 광선 교회·안내선 SVG 삽화), `?` 도움말에서 재실행.

### 3.4 "없는 정보" 처리 원칙

| 부족한 정보 | 앱의 동작 |
|---|---|
| 축척 | 배너 E05 + 토스트: "미터 거리를 표시할 수 없다" 는 사실, 이유(COLMAP 임의 단위), 해결 3가지. 거리·좌표에 `u` 단위와 ⚠, CSV 미터 열 공란·출처 열에 명시. 보정·직접 입력·사이드카로 해결되면 출처를 항상 함께 표시 |
| 광선 부족(<2) | E07 진행 불가 |
| 광선 각도 부족(<10°) | E08 확정 차단 + [그래도 확정(비권장)] / [마지막 광선 취소] 선택 제공 |
| 잔차 과대 | E09 경고, 점은 만들되 품질 "나쁨" 표시 |
| 위 방향 | E06 안내(측정 무관)와 기본 가정 명시 |
| 형식·속성 오류 | E02/E03/E04 에서 무엇이 들어 있는지(속성 목록)와 올바른 파일 안내 |

## 4. 검증

### 4.1 자동 테스트 (헤드리스 Chrome + SwiftShader, `puppeteer-core`)

합성 3DGS PLY: 한 변 **2.000 m** 정육면체 와이어프레임(45,120 가우시안, 꼭짓점 노란 마커, 회색 바닥). 스크립트가 카메라를 5개 시점에 놓고 꼭짓점의 투영 픽셀(±0.5 px 난수)을 클릭.

| 항목 | 결과 |
|---|---|
| 렌더링 ↔ 계산 좌표계 일치 | 투영 픽셀의 실제 색 = 노란 마커(255,255,51) → 렌더 프레임이 파일 좌표와 동일함을 확인 |
| 꼭짓점 (2,2,2) 5시점 측정 | 3D 오차 **2.5 mm**, σ₀ 1.3 mm, 잔차 RMS 0.25 px, 최대각 50°, 품질 좋음 |
| 꼭짓점 (0,2,2) 5시점 측정 | 3D 오차 **3.6 mm** |
| 두 꼭짓점 거리 (참값 2.000 m) | **2.0019 ± 0.0036 m** |
| 축척 없는 파일(`cube.ply`) | 배너 E05 표시, 단위 "모델 단위(u)", 토스트 E05·E06 |
| 같은 시점 2회 클릭 후 확정 | E08 차단(최대 각도 0.8°) + 대안 버튼 |
| 축척 직접 입력 | 단위 표시가 "미터 (m)" 로 전환 |
| 콘솔 오류 | 없음 |

실제 3DGS 샘플(진주 LH, 15만 가우시안): 로드 1.3 s(소프트웨어 렌더링 기준), PLY 헤더의 `units: meters`, `up axis: z` 자동 인식.

스크린샷: `webapp/docs/shots/` (01 시작 화면, 02 안내선, 03 거리 표시, 04 정보 점검, 05 축척 없음 배너, 06 E08, 07~09 샘플 렌더·확대창·자동 회전).

### 4.2 테스트 중 발견·수정한 문제

- `hidden` 속성 요소(모달·로딩·배너)가 CSS `display:flex` 에 덮여 항상 보임 → `[hidden]{display:none!important}` 추가.
- 시작 화면이 상단 도구막대를 가려 버튼 클릭 불가 → 도구막대를 시작 화면 위로 올림.
- 자동 정밀 보정이 시점 차가 큰 경우 1~2 px 편향 유발 가능 → 유사도 개선 여유 0.05, 3번째 이후 이동 8 px 제한으로 보수화.
- 초기 프레이밍이 멀리 떠 있는 배경 가우시안에 휘둘림 → 거리 75 퍼센타일 사용.

### 4.3 검증하지 못한 것 (2026-09-06 보완)

- 헤드리스 Chrome 을 실제 GPU(TITAN RTX, ANGLE Vulkan)로 띄우면 **줌과 무관하게 3DGS 로드 직후 WebGL 컨텍스트가 손실**되어(회전만 해도 동일, 정육면체·샘플 모두) GPU 가속 상태의 화면 검증은 불가했다. 소프트웨어 렌더러(SwiftShader)에서는 손실이 없고 모든 로직 테스트가 통과했다. 사용자 실제 Chrome 에서는 렌더·줌이 동작했으므로 헤드리스 Vulkan 환경 문제로 판단한다.
- 소프트웨어 렌더러로 실제 샘플(15만 가우시안)에 근접 줌하면 프레임당 10초 이상 걸려 자동 테스트 탭이 종료되었다(이전 "Target closed" 의 원인). 실제 GPU 에서는 해당 없음.

- 실제 GPU·실제 사람의 마우스 조작(헤드리스 소프트웨어 렌더링과 프로그램 클릭으로 대체). 확대창·투어의 시각적 배치는 스크린샷으로만 확인.
- 대용량(250만 가우시안, 600 MB) PLY 의 브라우저 메모리·속도: 데스크톱 Chrome 에서 가능할 것으로 예상하나 미측정. SPZ/SPLAT/KSPLAT/SOG 실제 파일 로드는 Spark 문서 기준이며 직접 시험하지 않음.

## 4.4 사용자 보고 문제와 수정 (2026-09-05 저녁)

**증상**: 버튼(파일 열기·파일 선택·샘플)이 반응하지 않고, PLY 를 끌어다 놓으면 파일이 새 탭에 텍스트로 열림.
**원인**: `webapp/index.html` 을 더블클릭해 `file://` 로 연 것. Chrome 은 `file://` 원본에서 외부 모듈 스크립트(`app.js`) 로드를 CORS 정책으로 차단한다(헤드리스 재현: "Cross origin requests are only supported for protocol schemes: http, https…"). 스크립트가 전혀 실행되지 않아 드롭 핸들러도 없었고, 브라우저 기본 동작으로 파일이 열렸다. GitHub Pages 도 아직 켜져 있지 않아(404) 온라인 경로도 없었다.

**수정**
1. **단일 파일 버전** `webapp/dist/3DGS_거리측정기_단일파일.html` (6.8 MB): esbuild 로 three·Spark·앱 코드를 한 번들로 묶어 CSS 와 함께 HTML 에 내장(`webapp/build_single_file.mjs`). Spark 는 워커를 Blob URL 로 만들어 번들 안에서도 동작함을 확인. `file://` 로 열어 전체 E2E 통과.
2. `index.html` 을 `file://` 로 열면 인라인 스크립트가 원인과 세 가지 해결 방법(단일 파일·로컬 서버 스크립트·온라인)을 화면에 표시하고, 드롭 시 파일로 이동하는 기본 동작을 차단.
3. **실행 스크립트** `tools/start_webapp.sh`: 로컬 서버를 띄우고 브라우저를 자동으로 연다.
4. `help.html` 의 오류 표를 빌드 시 정적 HTML 로 굽기(모듈 스크립트 제거) → `file://` 에서도 표시.
5. (빌드 과정 버그) 번들을 HTML 에 넣을 때 `String.replace` 의 문자열 치환이 `$$` 를 `$` 로 해석해 `$$` 헬퍼가 `$` 를 덮어쓰는 문제 발견 → 함수 치환으로 수정 후 `file://` 전체 E2E 통과(3D 오차 4.4~5.2 mm, 거리 2.0028 ± 0.0026 m).
6. 큰 파일 경고(E13) 기준을 300만 가우시안 또는 700 MB 로 낮추고 예상 메모리·해결 명령을 표시. 사용자가 열려던 DS003 PLY(1.4 GB, 586만 가우시안)는 GCP 축척 9.7429 를 적용해 미터 단위·150만 가우시안·SH0 으로 축소한 브라우저용 파일을 `data/ds003/` 에 준비.

## 4.5 사용자 보고 문제 2: 휠 확대가 일정 수준에서 멈춤 (2026-09-05 밤)

**증상**: Chrome 에서 휠 확대가 어느 수준 이상 되지 않음. VS Code 내장 브라우저에서는 더 확대됨.
**원인(재현 측정)**: three.js OrbitControls 의 커서 줌은 "궤도 중심까지의 거리"를 휠 한 칸당 5 % 줄이며 커서 방향으로 이동할 뿐 **표면의 위치를 알지 못한다**. 정육면체 꼭짓점을 향해 휠 20회를 보내자 카메라가 꼭짓점 2.5 cm 앞까지 간 뒤 **그대로 통과**해 40회에 1.43 m, 60회에 1.94 m 뒤로 멀어졌다. Chrome 은 휠 한 칸 delta 가 커서 표면 근처에서 한 칸이 10 cm 이상 점프하므로 "더 확대되지 않는다"고 느껴지고, VS Code 브라우저는 delta 가 작아 더 미세하게 접근된 것이다.
**수정**: OrbitControls 의 줌을 끄고 자체 휠 핸들러 구현. 로드 시 저장한 가우시안 중심 표본(최대 20만 점)에서 커서 광선 주변(반경 10 px 원뿔, 부족하면 25·62 px 로 확대) 점들의 깊이 15 퍼센타일을 **표면 깊이**로 추정하고, 남은 거리의 12 %씩 접근(최소 간격 = 장면 반경의 0.2 %)하여 표면을 절대 통과하지 않게 했다. 궤도 중심은 표면 깊이에 두어 회전이 그 점을 중심으로 이루어지고, near/far 평면을 카메라–중심 거리에 비례해 매 프레임 조정해 근접 시 잘림을 없앴다. deltaMode(픽셀/줄/페이지) 정규화로 브라우저 차이도 흡수.
추가로 확인·수정한 것: (a) 휠 이벤트의 커서 좌표는 정수 픽셀이라 광선이 목표점을 0.5 px 비껴가고, 접근할수록 그 어긋남이 화면에서 커져(거리에 반비례) 목표점이 커서에서 미끄러진 뒤 표면 추정이 뒤쪽 배경으로 넘어가는 현상 → 깊이 대신 **커서 아래 표면 점 군집의 3D 중심점**을 향해 이동하도록 변경(보고 있던 표면 점이 화면에 고정). (b) OrbitControls 는 생성 시점의 up 으로 회전축을 고정하므로 [위 방향]을 바꿔도 회전축이 옛 축(−Y)이던 버그 → up 변경 시 내부 회전축 갱신.

**검증(수정 후, 헤드리스 Chrome)**: 정육면체 꼭짓점을 향해 휠 60회 → 카메라–꼭짓점 거리 4.07 m → 1.14(10회) → 0.325(20회) → 0.096(30회) → 0.031(40회) → **0.012 m(60회)** 로 단조 감소, 통과 없음. 휠 반대로 12회 → 0.031 m 로 후퇴. Z-up 에서 왼콽 드래그 시 카메라 높이 변화 0 (회전축 정상). 단일 파일(`file://`)·http 모두 PASS, 측정 회귀 테스트도 PASS(3D 오차 1.8~8.7 mm). 실제 샘플(진주 현장)에서 궤도 반경 150 m → 44 → 13.6 m 로 표면 감지 접근 확인.

## 4.6 기능 추가: 확대창 크기·고해상도, 클릭 후 자동 회전 (2026-09-19)

**요청**: 확대창의 확대 정도(크기)를 3단계로 조절, 해상도가 낮아 잘 안 보이는 문제 개선, 각도를 바꿔 같은 점을 다시 찍는 어려움을 줄이는 자동 시점 전환(켬/끔), 새 파일은 이름 뒤에 "자동회전".

**구현**
1. **확대창 크기 3단계** 소/중/대 = 160/260/380 px. 측정 패널(측정 중 바로 전환)과 설정 탭 양쪽에 세그먼트 버튼. 배율(2~8×)은 별도 유지.
2. **고해상도 렌더링**: 기존 방식은 화면 픽셀을 그대로 키워 배율이 높을수록 흐릿했다. 이제 `camera.setViewOffset` 으로 커서 주변 영역만 좁혀 별도 렌더 타겟(확대창 해상도 × DPR)에 다시 그린 뒤 확대창에 표시 → 진짜 확대. 실패 시 자동으로 픽셀 복사 방식으로 전환하고 확대창 하단에 "고해상도/픽셀" 을 표시. 설정에서 끌 수 있음.
3. **클릭 후 자동 회전**(기본 켬, 측정 패널·설정에서 끔): 첫 클릭 시 커서 아래 표면 점(가우시안 군집 중심)을 피벗으로 잡고, 클릭마다 위(up)축 기준 **350° ÷ 광선 수**(기본 70°)씩 궤도 회전(0.7초 애니메이션, 거리·고도 유지, 피벗은 화면 중앙에 고정). 2번째 클릭부터는 피벗을 최소제곱 추정점으로 갱신. 회전 중 클릭은 무시하고 안내. <kbd>R</kbd> 은 같은 각도만큼 한 번 더 회전(가려진 경우). 끄면 이전 동작(첫 클릭 지점으로 화면 중심만 이동).
4. **클릭 판정 수정**: "누르고 떼기 600 ms 이내" 조건 때문에 렌더링이 느린 순간의 클릭이 드래그로 오인되어 무시되는 문제 발견 → 이동 거리(5 px)로만 판정.
5. 설정을 브라우저에 저장(다음 방문 복원). 빌드 출력은 **`dist/3DGS_거리측정기_단일파일_자동회전.html`** 로 분리(이전 판 유지).

**검증(헤드리스 Chrome, 정육면체 2.000 m)**: 자동 회전 켬 상태에서 꼭짓점을 실제 마우스 클릭 5회(각 클릭 후 회전 완료 대기, 회전 후 꼭짓점이 화면 중앙 (530,402) 에 정확히 위치) → 방위각 변화 70.0°×4, 최대 광선각 125°, 3D 오차 0.0000 m(프로그램 클릭이라 조준 오차 없음). 확대창: 중(260 px) → 대(380 px) 전환, 고해상도 모드 동작(밝은 픽셀 59,828개). 측정 회귀 PASS(http·file://). 4번째 회전이 10.7초 걸린 것은 소프트웨어 렌더러 프레임 시간이며 애니메이션 자체는 0.7초.

## 4.7 기능 추가: 기준점(GCP) 좌표 보정, 회전 방향 제어 (2026-09-19, 방향제어 판)

**요청**: 축척 보정 화면이 헷갈림 — GCP 좌표를 단위와 함께 입력하면 계산되게; 자동 회전이 기대한 방향이 아님 — 보는 방향 기준으로, 측정할 점을 중심으로 돌고 원하는 방향을 입력할 수 있게; 새 파일명에 "방향제어".

**구현**
1. **축척 보정 화면 재구성**: 첫 화면에서 ① 길이 하나로 / ② 기준점(GCP) 좌표로 / ③ 축척 직접 입력 중 고르고, 각 카드에 무엇을 입력하는지 설명. ③ 문구를 "모델 1 u = 실제 몇 m" 로 명확화.
2. **② 기준점 좌표 보정**: 측정한 점 목록 옆에 실제 X·Y·Z 입력란, 입력 단위(m/cm/mm), 좌표계 이름. 2점 → 축척(실거리÷측정거리). 3점 이상(공선성 검사 통과) → Horn(1987) 단위쿼터니언 최소제곱으로 축척+회전+이동(4×4 대칭 행렬 Jacobi 고유벡터). 점별 잔차·RMS 표시, 잔차 과대 경고. 적용 시 `state.unit.transform` 에 저장되어 모든 좌표 표시·CSV(x_real,y_real,z_real,crs)·사이드카 JSON 에 실좌표가 나오고, 브라우저에 파일별로 기억.
3. **회전 방향 제어**: 회전축을 화면 기준(카메라의 세로·가로축)으로 바꾸고, 패턴(오른쪽/왼쪽 계속, 좌우 번갈아 +α −α +2α −2α, 위아래 번갈아 ±70° 한도)·각도(비우면 350°÷N)·축(화면/파일 up) 을 측정 패널과 설정에서 선택. 측정 중 ← → ↑ ↓ 키로 한 단계 직접 회전(상하는 극점 통과 방지). 코치 바에 다음 회전 방향·각도 표시.

**검증(헤드리스 Chrome)**: 정육면체 4점의 실좌표를 축척 9.742857·Z축 30° 회전·이동(285889.063, 121060.398, 40) 으로 만들어 입력 → 복원 축척 9.74286, RMS 0.000 m, 점 실좌표 오차 1.6e-11 m; 2점 입력은 축척만 모드로 정확. 화면 기준 회전: 오른쪽 70° → 카메라가 화면-오른쪽으로 4.72 m 이동(위 이동 0), 피벗 화면중심 오차 0.6 px, 거리 유지; 위 30° → 화면-위 0.80 m; ↓ 키 회전 동작. 측정 회귀·자동회전 5클릭 테스트 PASS(file://).

**남은 한계**: 화면 기준 회전 후 OrbitControls 가 화면의 기울기(roll)를 파일 up 기준으로 다시 맞추므로, 파일의 위 방향이 맞지 않은 모델에서는 연속 회전 시 궤도면이 조금씩 달라질 수 있습니다. 그 경우 [위 방향]을 먼저 맞추거나 축을 "파일 위 축"으로 두는 것이 안정적입니다.

## 4.8 기능 추가: 플로팅 조작 패널 (2026-09-19, 조작메뉴 판)

**요청**: 회전·확대·축소를 마우스 대신 HTML 페이지 안의 플로팅 조작 메뉴로도 할 수 있게, 별도 파일명으로.

**구현**: 3D 화면 위에 드래그 가능한 패널 `#navpad`. 회전 ◀▶▲▼(궤도 중심 기준 화면 축 회전, 한 번에 15°, 설정 가능, 극점 통과 방지), 초점 ◎(측정 중인 점), 확대 ＋－(휠과 같은 표면 인식 `zoomAt` 을 화면 중앙에 적용 → 표면 통과 없음), 이동 ←→↑↓(화면 기준 평행이동, 궤도 중심 거리의 12 %), 홈 ⌂. 버튼을 0.3 초 이상 누르면 45 ms 간격 미세 연속 동작(회전 2°, 이동 2 %, 확대 0.4 칸). 제목 드래그로 위치 이동(브라우저 기억), ▾ 접기, ✕ 숨김·도구막대 [🎮 조작 패널]·설정으로 토글. 휠 핸들러를 `zoomAt(px,py,delta)` 로 분리해 재사용. 빌드 출력 `dist/3DGS_거리측정기_단일파일_조작메뉴.html`.

**검증(헤드리스 Chrome)**: ▶ 15.0° 회전·거리·중심 유지, ▲ 15.0°, ＋ 거리 6.30→5.50, － 5.50→6.26, → 중심 이동 0.750(=12 %), ◀ 0.9 초 누르기 39.5° 연속, ⌂ 전체 보기, 접기·드래그 200 px·토글 숨김 정상, 오류 없음. 측정 회귀 PASS(file://).

## 4.9 기능 추가: 지역·국가 좌표계(큰 좌표) PLY 자동 원점 이동 (2026-09-19)

**요청**: GCP 보정으로 지역 좌표계(EPSG:5186, X≈285,900 m)를 쓰는 3DGS PLY 가 뷰어에서 열리지 않아 매번 파일을 offset 해 저장했던 문제를 뷰어 쪽에서 해결.
**원인**: GPU 렌더러(Spark)는 위치를 float32 로 다루며, 285,900 에서 float32 의 최소 간격은 약 3 cm 라 스플랫 위치·정렬이 깨지고 경계 계산도 불안정.
**구현**: PLY 헤더 해석 후 위치 표본의 중앙값이 2,000 u 를 넘고 장면 반경의 200배 이상이면 정수로 반올림한 오프셋을 정해 버퍼 안의 x,y,z(압축 PLY 는 chunk min/max)에서 빼고 렌더러에 넘김(`shiftPlyInPlace`). `state.coordOffset` 에 기록하고 `origCoord()` 로 복원 → 좌표 표시, CSV(x_model 열도 원래 값), JSON(`p` 원래, `p_viewer` 내부), 기준점 보정 입력·사이드카 변환은 모두 원래 좌표 기준. 정보 점검에 오프셋 표시, E14 안내 추가. 거리 계산은 평행이동 불변이라 영향 없음.
**검증**: 합성 정육면체에 (285889.063, 121060.398, 40) 오프셋을 더한 PLY → 오프셋 (285890, 121061, 40) 자동 감지, 측정 좌표 표시 (285891.063, 121062.398, 42.000) m 로 원래 값 복원(오차 0.000·0.008 m; 파일 자체가 float32 라 ~3 cm 정밀도), 거리 1.9935 m, GCP 2점에 원래 좌표를 넣으면 축척 1.003·잔차 0.002 m. 실제 DS003 CM001 절대좌표 모델(80만 가우시안 축소본, 중앙값 285,900/121,054) 로드·렌더 확인. 파일명은 변경 없이 `…_조작메뉴.html` 갱신.

## 4.10 조정: 확대창 기본 배율 4× → 2× (2026-09-19)

사용자 요청으로 확대창 배율을 절반으로 낮춤. 창 크기(소/중/대)는 유지. 슬라이더 범위 1~8×(0.5 단위), `[` `]` 키 0.5 단위. 이전에 브라우저에 저장된 설정(4×)은 설정 버전(v2) 이전으로 한 번 2× 로 바뀜. 검증: 옛 설정 저장 상태에서 새로고침 → 2×, 창 260 px 유지.

## 4.11 조정: 확대창 배율 범위 2~5× (2026-09-19)

사용자 요청으로 배율 조절 범위를 2~5×(0.5 단위)로 확정. 측정 패널 확대창 소/중/대 옆에 배율 선택(2×…5×) 추가, 설정 슬라이더·`[` `]` 키 범위 동기화, 저장된 값이 범위 밖이면 보정. 검증: 저장값 8 → 5 로 보정, 패널 선택 3.5, 키로 최대 5·최소 2, 5× 확대창 표시 PASS.

## 4.12 기능 추가: 분석 1단계 — 각도·수평/수직/경사·길이·면적 (2026-09-19)

**구현**: 도구막대 📐 분석 메뉴(각도 A / 길이 L / 면적 P). 작업 종류 `angle`(3점 자동 완성) · `polyline`(≥2, 완성 버튼/Enter) · `area`(≥3). 점은 새로 측정(5회 클릭)하거나 **기존 점 마커 클릭으로 재사용**(`pickExistingPoint`, 14 px). 계산: 각도 = acos(BA·BC); 길이 = 구간 합; 면적 = 최소제곱 평면(3×3 대칭 Jacobi 고유분해)에 투영한 다각형의 신발끈 면적 + 수평투영 면적·둘레·면 기울기·평면 잔차 RMS; 거리 분해 = 위 축 기준 수평 성분·고저차·경사(°, %). **오차**: 점 좌표 함수의 수치 야코비안으로 각 점 공분산을 전파(`propagate`). 오버레이(다각형 채움·호·라벨), 결과 탭 분석 표, 거리 표 열 확장, CSV `analysis` 섹션·JSON `geometries`, 점 삭제 시 종속 항목 정리, 중복 선택 방지.
**검증(정육면체)**: 각도 90.00° ± 0.01°, 길이 P1→P2→P3 4.000 m, 면적 윗면 4.000 m²(둘레 8.000, 기울기 0.0°, 잔차 0), 거리 분해 (0,0,0)–(2,0,2): 2.828 / 수평 2.000 / 고저차 2.000 / 45.0° / 100 %. 기존 점 마커 클릭 흐름·완성 버튼·중복 방지·삭제 정리 PASS, 측정 회귀 PASS(file://).

## 4.13 기능 추가: 점군 보기 · 1클릭 직접 선택 · 정밀화 (2026-09-19)

**요청**: CloudCompare처럼 PLY 를 점군으로 보고, 점을 바로 선택해 측정에 쓰며, Tab/버튼으로 스플랫과 전환. 권장안(군집 중앙값 기본 + 최근접 옵션) 채택, 두 방식의 차이를 설명하는 장치 포함.
**구현**
- 점군: PLY 파싱 시 위치·SH0 색·불투명도(5 % 미만 제외)로 `THREE.Points` 생성(`buildCloudFromPly`; 다른 형식은 Spark `forEachSplat` 로 추출). 보기 모드 splat/cloud/both(Tab·메뉴), 점 크기·색(RGB/높이/단색). 휠 줌의 표면 추정 표본도 이 점군에서 추출.
- 직접 선택 `directPick`: 클릭 광선 원뿔(반경 설정 3~20 px, 없으면 2배씩 확대) 안의 중심점을 전체 점군에서 탐색(4.5만~수백만 점 브루트포스, 수십 ms). **cluster**: 거리순 정렬 후 12 % 깊이 띠 안에 K점(3 또는 3 %) 이상 모인 첫 무리의 좌표 중앙값, σ = 무리 퍼짐; **nearest**: 각도상 가장 가까운 한 점, σ 0. 점에 `method:'pick'`, `pickMode`, 품질 배지 `직접선택`.
- 라우팅: 점군/겹침 보기이거나 설정 "스플랫에서도 1클릭"이면 첫 클릭이 직접 선택; Shift+클릭은 다시점 광선; 분석 작업에서 기존 마커 클릭은 여전히 재사용.
- 정밀화 `startRefine`: 직접 선택 점을 피벗으로 다시점 측정을 시작해 완료 시 같은 점(id·이름 유지)을 교체, 종속 거리·분석 자동 갱신.
- **설명 장치**: ① 설정의 두 선택지마다 설명문(카드형 라디오) ② [? 설명] 버튼 → 비교표 모달(상황별 장단점, 권장, 논문의 지적) ③ 처음 직접 선택 시 그 모달 자동 표시 1회 ④ 결과 표 배지 툴팁·범례 ⑤ 코치 바에 현재 방식과 Shift 안내 ⑥ 도움말·README·사용법 표. CSV `method` 열, 정보 점검에 점군·방식 표시.
**검증(정육면체)**: Tab 전환 후 점군 렌더 확인(표본 3/3), 군집 중앙값 선택 오차 0.2 mm(군집 77점, σ 13.7 mm), 최근접 4.9 mm, 꼭짓점 앞 0.5 m 에 잡티 2점 삽입 시 군집 방식 오차 0.0 mm(무시 확인), 설명창 첫 표시, Shift+클릭 광선 1개, 정밀화 후 multi/good·오차 0.0 mm·점 개수 유지. 회귀·분석 테스트 PASS(file://).

## 5. 한계와 향후 과제

- 축척은 여전히 외부 정보(실측 길이·GCP·GPS)가 필요합니다. 앱은 "쉽게 한 번" 넣게 할 뿐입니다.
- 결과 좌표는 파일 좌표계입니다. 절대 좌표가 필요하면 `tools/gs_ply_georef.py` 로 지오리퍼런싱 후 열어야 합니다.
- 자동 정밀 보정은 시점 차가 크면(무늬 변형) 동작하지 않도록 설계되어, 큰 각도에서는 사람의 클릭에 의존합니다. 향후: 아핀 보정 템플릿 매칭, 다중 스케일 매칭.
- 향후: 면적·체적, 여러 파일 비교, 드론 사진+COLMAP 으로 브라우저 안에서 축척 자동 추정(현재 파이썬 스크립트 기능의 JS 이식).

## 6. 배포 방법 (완료 상태)

1. **온라인**: `webapp/` 폴더를 `git subtree split` 으로 `gh-pages` 브랜치로 만들어 푸시 → GitHub Pages 가 자동 활성화됨. 주소 **https://rocheol2.github.io/3DGS_Accurate_Point_Mesurement/** (2026-09-05 22:58 배포·헤드리스 확인: 초기화·샘플 로드 정상). 재배포: `tools/publish_pages.sh`.
2. **더블클릭**: `webapp/dist/3DGS_거리측정기_단일파일.html` (빌드: `webapp/build_single_file.mjs`).
3. **로컬 서버**: `tools/start_webapp.sh` → `http://localhost:8000/`.
4. `main` 브랜치 루트의 `index.html` 은 `webapp/` 으로 리다이렉트(Pages 소스를 main 으로 바꿔도 동작), `.nojekyll` 포함.

## 7. 사용 라이브러리·라이선스

- Spark 2.1.0 (World Labs, MIT) — `webapp/vendor/LICENSE-spark.txt`
- three.js 0.180.0 (MIT) — `webapp/vendor/LICENSE-three.txt`
- 테스트: puppeteer-core(개발용, 저장소 미포함), Google Chrome headless

## 8. 산출물 목록

| 경로 | 내용 |
|---|---|
| `webapp/` | 웹앱 전체(정적) · `dist/3DGS_거리측정기_단일파일.html` 더블클릭용 · `build_single_file.mjs` 빌드 스크립트 |
| `tools/start_webapp.sh` | 로컬 서버 + 브라우저 자동 실행 |
| `webapp/help.html` | 웹 사용 설명서 |
| `웹앱_사용법.md` | 사용법(마크다운) |
| `웹앱_작업결과_보고서.md` | 이 문서 |
| `index.html`, `.nojekyll` | GitHub Pages 진입 |
| `webapp/docs/shots/*.png` | 검증 스크린샷 |
